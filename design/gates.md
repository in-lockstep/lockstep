# in-lockstep exit gates

Every gate referenced by id in the pivot plan, defined here. A gate named in a phase table and
defined nowhere is indistinguishable from a gate that does not exist. That was the weak point of
two review rounds (R6-QA-5), so this file is a Phase-0 deliverable and the phase tables reference
it rather than restating it.

`(Pn)` is the phase whose exit the gate blocks. `(Pn-m)` means it holds continuously across a range.

`design/objectives.md` is the ledger one level up, and it reads this file. Each of the ten
objectives in `CLAUDE.md` names the gates that carry it and the gates it is blocked on, and
`GATE-TEST-8` fails when the two files disagree — so a gate here changing status is a gate that
has to be accounted for there. That join is why an `unmet` row is worth writing before anybody
intends to implement it: it is how a gap becomes something an objective can point at, instead of
prose in a review nobody re-reads.

## Status, and why this column exists

The preamble above says a gate defined nowhere is indistinguishable from one that does not exist.
The same argument runs one step further, and this file did not make it: **a gate that is defined,
and whose mechanism is unit-tested, and which nothing invokes during a run, is also
indistinguishable from one that does not exist.** Nine rows below were in that state while
`docs/controls-crosswalk.md` cited them as evidence that a substrate control had been replaced.

| Status | Meaning |
|---|---|
| `held` | The mechanism runs on a live path and an assertion covers it. |
| `unit only` | The mechanism exists and is tested in isolation. Nothing calls it during a run, so the assertion, which is about a run, is not established. **Not a pass.** |
| `partial` | Some clause of the assertion is covered and some is not; the row says which. |
| `unmet` | No mechanism, or a mechanism with nothing asserting over it. |
| `deferred` | Past the 1.0 cut line, by the deferral recorded at §17.11 of `design/in-lockstep-design.md` — which names `ctx.park` and `ctx.fan_out` as not on `RunContext` at 1.0, and is what every `deferred` row here rests on. |
| `retired` | The subject was deleted; the row records what it held and why it stopped. |

`unit only` is the status worth understanding, and `GATE-EGRESS-1` was the example. It had a
thorough test that a `ContextPackage` carrying untrusted content under `EgressMode.NONE` raises
`EgressRefused`. That is a real test of a real mechanism. But `EgressPolicy.check()` had no
caller outside that test, so no run had ever been refused and the gate's actual assertion
("BLOCKED before the first model call") had never been exercised. It is `held` now, because
`AiInvoker.run` calls it.

The distinction this project already draws between `decided` and `succeeded` is the same one: a
mechanism nobody invoked is outstanding, not passed.

One row is in that state — `GATE-LEDGER-2`; `GATE-RETRY-5` stood beside it until #265 retired it with its subject — and `GATE-OUT-2` was `partial` beside it until #267 struck the two clauses that named a `Cache`
nobody wrote. Each row says what is missing. `GATE-EVIDENCE-1` stood beside it until #268 promoted the first case; its row
says what one promotion does and does not establish.

They were found by re-checking every `held` row for a call site, which is the exercise the column
was created by and had not been repeated since; 5 of 69 did not survive it. Four of the five were
wrong about nothing but their own status. The fifth, `GATE-EVAL-1`, also gave a reason that was
never true here — it credited a progressive-disclosure hole that this codebase does not have — and
that went unnoticed for the same reason the status did: a row over a mechanism nobody calls is a
row nobody has occasion to check. Two of those five — `GATE-LEDGER-4` and
`GATE-EVAL-1`, both over `EvalSubject` — are `held` again, because a run now computes a subject and
the record keeps it. That round trip is what the column is for: the rows said `unit only` for
exactly as long as it was true, and moving them back cost a call site rather than an edit.

The pair named here before was `GATE-APPROVAL-1` and `GATE-POLICY-1`. Both are `held` now, and this
sentence went on naming them anyway — the decay described one paragraph up, arriving in the
paragraph that describes it. A status column nobody re-checks is what this file is about, and so is
a sentence about one.

Nothing enforced this file's *primary key* either, until `GATE-TEST-7`. `GATE-EVAL-1` named two
unrelated properties from #136 until #213, and because `test_gates.py` searches the test tree as one
concatenated string, each row was discharged by the other's test: delete either test and both rows
stay green. The unmet half of the ratchet is worse under a duplicate — an id some unrelated test
already cites can never be marked `unmet`, so the only status it can carry is a reassuring one.

## Async and concurrency

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-ASYNC-1` | P0 | held | AST scan of `in_lockstep/llm/providers/*.py`: every client constructor names a class matching `^Async`, or is `httpx.AsyncClient`. |
| `GATE-ASYNC-2` | P0 | held | `asyncio.wait_for(provider.generate(...), 0.1)` against a 5s stub raises `TimeoutError` AND the stub records connection-closed-before-completion. |
| `GATE-ASYNC-3` | P1 | held | With `IN_LOCKSTEP_DISABLE` set mid-run, a workflow with steps remaining reaches a terminal `Outcome` without executing another adapter. (Arbitration wrote this against `fan_out`, which is post-1.0 by the §17.11 cut line; the branch variant is `GATE-ASYNC-3b`.) |
| `GATE-ASYNC-3b` | P10 | held | With `IN_LOCKSTEP_DISABLE` set mid-run, an in-flight 3-branch `fan_out` reaches a terminal `Outcome` within 2s. Off the §17.11 cut line by the 2026-09-06 decision that builds the six deferred gates (`.plan.md`, Phase 5). Each branch is an asyncio task; a watcher polls the switch every 50 ms and cancels what has not finished, and a cancelled branch joins as `BLOCKED`/`killswitch` rather than as an exception, because the switch is a control working and the join has to say so per branch. A branch not yet started when the switch is thrown is refused by `run_call` the way any step is (`GATE-ASYNC-3`), and a branch inside a model loop is also stopped by the invoker's own per-turn check, which is the second reach a session a model delegates to (#332) will have. Asserted with two branches asleep for ten seconds and a third that throws the switch after a tenth of one: the join is terminal inside two seconds, all three `killswitch`, the run's steps say so, and no task outlives the call. |
| `GATE-ASYNC-4` | P2 | held | Three concurrent `generate()` calls complete in under 2x the time one takes, against the **real** provider code rather than a stand-in: `ClaudeTransport.generate` with its SDK client replaced (the shared path for Anthropic, Bedrock and Vertex, with a companion test failing if any of the three grows its own `generate`), and `OllamaProvider` end to end through its own `httpx` path. Paired with a negative control: the same transport with a synchronous wait inside `async def`, which must fail the assertion, or the gate proves only that asyncio exists. |

## Cost

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-COST-1` | P1 | held | No module-level mutable accumulator exists under `in_lockstep/`; two concurrent `RunContext`s accumulate `Spend` independently. |
| `GATE-COST-2` | P2 | held | Stub charges proportional to **cumulative** input tokens, never a flat per-turn fee (flat is the one curve under which quadratic growth is invisible): with `budget=$0.10` the run is `BLOCKED` before the turn whose *estimate* would cross, and a `len/4` estimator fails this gate. |
| `GATE-COST-3` | P2 | held | A model absent from `CostTable` yields `Outcome(BLOCKED)` with finding id `cost.unpriced_model` and **zero** `generate()` calls. |
| `GATE-COST-4` | P0 | held | The identifier `DEFAULT_COST_PER_M` appears nowhere under `in_lockstep/`. |
| `GATE-COST-5` | P3 | held | `in-lockstep doctor` exits non-zero when no provider org spend limit is attested in config. |
| `GATE-COST-6` | P10 | held | A 4-branch `fan_out` under a joint `$1.00` `Spend` charges ≤ `$1.00` in aggregate, not per branch. Off the §17.11 cut line with `GATE-ASYNC-3b`. The mechanism is `Spend.reserve`: the projection is checked and reserved in one synchronous step -- no `await` between them, so under asyncio two branches cannot interleave inside it -- and released by the charge, or by `release()` when the call never happened. `would_exceed` followed by a call was the primitive before, and it is the negative control the test keeps: four branches each projecting thirty cents against one dollar all see the same remaining dollar, all four go, and $1.20 is charged. With reservation, three go and the fourth is refused before its call. Asserted twice: at the context, over branches shaped like a model turn, and at the invoker, where two sessions on one `Spend` whose ceiling fits one more turn produce exactly one refusal and one provider call. The same `Spend` and the same primitive are what a session a model delegates to will share (#332): a child cannot exceed the parent's remaining budget because there is one budget. |
| `GATE-BUDGET-1` | P1 | held | A run with no declared budget is refused at startup. `Lockstep.context()` raises `UndeclaredBudget`, surfaced by the CLI as a message rather than a traceback. Scoped to lifecycles that can spend: the trigger is a bound adapter declaring `Capability.SPENDS_BUDGET`, so a repository binding only `Test` and `Validate` needs no ceiling. Any of the four `Budget` dimensions satisfies it, as does a `CostBudget` in the middleware chain, since the scaffold declares it that way. Two defaults were removed to make it satisfiable at all: `--budget` no longer has one, and `_default_lockstep` no longer injects `CostBudget(usd=2.00)`. A ceiling the CLI invents is a budget nobody chose, and every run would have arrived bounded by it. A run that cannot spend is not exempted; it declares a ceiling of zero, which the gate accepts and `Spend` enforces. `--offline` and `--dry-run` bind providers that transmit nothing and `apply` binds no model, so the CLI states zero for each (`_declare_zero_ceiling`). #174 was five clean installs refused for spend a cassette cannot incur. |
| `GATE-DEADLINE-1` | P2 | held | An `InvokePolicy` deadline expiring mid-loop yields `BLOCKED(reason="deadline")` with no further `generate()` calls; `KillSwitch` set mid-loop does the same. |
| `GATE-PROGRESS-1` | none | held | A session that stops making progress is stopped, as `blocked`, before it runs to whichever of the other ceilings comes first. Five `/fix` runs on this repository's own #319 are the evidence (#337): each staged a correct fix, one in 68 turns, and two spent 159 and 161 turns -- most of them reading, searching and rerunning the suite over the same staged set -- and ended at the wall ceiling, because raising a ceiling gives a wandering run more to use and a good run nothing. Progress is arithmetic (O7): the tool runner counts a write or delete the workspace ACCEPTED and a `run_tests` over a change set it had not run before; a write `ChangeGuard` refused, a `run_tests` over the same staged set, and every read, search and `run_script` count for nothing, and what the model says counts for nothing. `AiInvoker` reads the counter before and after each turn's calls, and at `max_idle_turns` consecutive turns without movement returns `stalled` rather than calling again; `run_phase` maps that to `BLOCKED(<verb>.no_progress)` with the finding naming the idle count, the ceiling and the last productive turn ("turn 41: wrote src/x.py"), and the outcome carries what was staged, so a run stopped for idling still hands a person its attempt. The ceiling is the fourth on `Workshop` and `InvokePolicy` and composes like the other three: the lower of the adapter's and the policy stack's. **Twenty after the first write, and twice that before it.** The sixth `/fix` on #319 (run 34152787128) was stopped at turn 20 with nothing staged, 88 seconds and $0.71 in, still reading the code it was about to change -- the reading phase this row had promised not to cut off, cut off, because the number was set from the clean run's 68 turns without knowing how many came before its first write. A session has to read before it can write, and the first write is where reading ends: before it the allowance is doubled, after it twenty turns without another write or a new suite run is wandering. A session that reads for forty turns and writes nothing is the #139 case, and stops there rather than at the turn cap. **The boundary:** a tool runner that keeps no counter -- an adopter's own -- reports nothing, and a ceiling over nothing does not fire, so the stop is real for the shipped `ToolRunnerImpl` and absent, by name here, elsewhere; the ledger record carries the finding and `implement`/`fix` print `N idle at the end` beside the turn count, but the record has no turn count of its own to put the idle count beside. |

## Retry

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-RETRY-1` | P2 | held | A transport stub returning 500 forever is called exactly 3 times for one logical model call. |
| `GATE-RETRY-2` | P0 | held | `with_retry` is imported nowhere under `in_lockstep/`; every SDK client is constructed with `max_retries=0`. |
| `GATE-RETRY-3` | P0 | held | A 400 error whose message contains `"generated"` maps to a non-retryable class and is attempted exactly once. (Today `"rate" in msg.lower()` matches "gene**rate**d".) |
| `GATE-RETRY-4` | P2 | held | `Retry-After: 30` is slept; `Retry-After: 3600` with 60s remaining wall clock returns `ERRORED` without sleeping. That is asserted **through `AiInvoker`**, which supplies the remaining budget, not against a `RetryPolicy` handed one by the test. The original form passed for the whole pivot while nothing on any live path set the field, so the gate proved the policy honours a budget it was never given. Successive sleeps are bounded in aggregate, not individually. |
| `GATE-RETRY-5` | P1 | retired | ~~`Retry` middleware invokes an action declaring `Capability.SPENDS_BUDGET` exactly once and emits a finding explaining the refusal.~~ **Retired with its subject (#265).** The class was tested and correct and constructed by nothing -- not `.lockstep/lockstep.py`, not the chain `init` scaffolds, not `examples/`, not `docs/` -- for the whole pivot, and the row above sat `unit only` as the only entry in the objective ledger's *claimed by no objective* section. The choice the issue posed was bind or retire, and binding was the worse half: what the class did was refuse to re-invoke every action that spends budget, which is every AI verb, and say so with a NOTE finding on every paid outcome -- a line in every review comment and every ledger record announcing that a control had done nothing -- while retrying the deterministic verbs on `ERRORED`, where a missing tool does not appear on the third attempt and a timed-out suite is re-paid in wall clock. Nothing it offered was wanted, which is why nothing bound it. The transport-level retry in `ai/retry.py` is the one that runs, bound inside every `AiInvoker` and held by `GATE-RETRY-1` through `-4`; `core/middleware.py`'s own argument -- retry belongs where one HTTP attempt is one HTTP attempt -- was the argument against this row all along. `RefusesBudgetedActions` went with it, a mixin nothing inherited, and `docs/extending.md` now states the check directly. |
| `GATE-RETRY-6` | P2 | held | A provider error carrying an API-key-shaped string produces an `Outcome` and ledger record matching no `Redact` secret pattern. Asserted for a seeded key and for an unseeded one caught structurally, in the `Outcome`, in the `Finding` it carries, in a rendered traceback (the chained cause is suppressed, or the original prints unredacted on a crash), and in the ledger file. Implementing it also closed a gap it depended on: a provider error escaped `AiInvoker.run` raw, so every adapter (all of which catch only `InvocationBlocked`) turned a 401 into a traceback rather than an `Outcome`. It is now `InvocationFailed`, mapping to `ERRORED` rather than `BLOCKED`, because §4.3 reserves `BLOCKED` for a policy refusal. |

## Test net

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-TEST-1` | P0 | held | `tests/characterization/` holds the composed prompts + sha256; re-running the current compiler reproduces them. |
| `GATE-TEST-2` | P5 | held | Each `in_lockstep`-composed prompt's ordered section-id list equals the corpus's; any byte delta has a committed `.diff` of identical hash. |
| `GATE-TEST-3` | P0 | held | `make cov` runs in CI; it fails below the committed floor, and above `floor+2` it fails **as a required floor-file update** ("coverage is X%, floor is Y%. Bump it"), not as a bare failure. `cov` must not inherit `[tool.coverage.report] omit` (`--cov-config=/dev/null`). The target was named `cov-new` while the old blended-package number was being replaced; this row said the old name for months after the Makefile stopped answering to it, which is the drift this file's own preamble warns about. |
| `GATE-TEST-4` | P0-7 | retired | ~~The golden tree's git hash equals the Phase-0 tag value.~~ **Retired at phase 7 with its subject.** It replaced the drift gate and protected generated output for the duration of the pivot; the compiler that generated that output is gone, so the gate has nothing to hold. `tests/characterization/` is what survives, and it protects the thing that outlived the emitter: the composition order. |
| `GATE-TEST-5` | P1-7 | retired | ~~`pytest --collect-only -q packages/pipeline-exec/tests` count ≥ the Phase-0 baseline.~~ **Retired with its subject.** `packages/pipeline-exec` was deleted after 1.0; the 478 tests this gate counted went with the code they covered. It existed so the pivot could not quietly erode the one test net that predated it, and it held for seven phases. The package was then removed deliberately, by decision, which is the outcome the gate was protecting the right to make. |
| `GATE-TEST-6` | P1-7 | retired | ~~`pyproject` `testpaths` still contains both `tests` and `packages/pipeline-exec/tests`.~~ **Retired with its subject**, for the same reason. It was `GATE-TEST-5`'s companion, guarding against the collection count being held up by a path that no longer ran. |
| `GATE-TEST-7` | none | held | No gate id appears twice in this file. The status column is a two-sided ratchet keyed on the id, and `_discharged` searches `tests/` and the `Makefile` as one concatenated string, so two rows sharing an id are each discharged by the other's test and neither can ever be marked `unmet`. `pytest.mark.parametrize` hides it rather than reporting it: colliding `ids=` are silently suffixed `0` and `1`, so the duplicate does not even show in the test output. Asserted with a positive control, because a uniqueness check over a regex that stopped matching would pass on an empty list — the same vacuity `test_the_ledger_is_not_empty` was already added to prevent one row up. |
| `GATE-TEST-8` | none | held | `design/objectives.md` and this file agree. The objective ids there are exactly `O1`-`O10`, each once, and their titles match `CLAUDE.md` verbatim; every gate an objective cites exists here; an objective marked `held` cites at least one gate and every gate it cites is `held`; a row that is not `held` names a gap rather than a dash; and that ledger's *claimed by no objective* section lists exactly the `unmet`, `partial` and `unit only` gates no objective cites; and its *held, and claimed by no objective* count is exact, so a `held` gate no row cites can neither join that set unnoticed nor leave it without the number being lowered (#273 counted thirty-nine, nearly half the held rows, that the unsettled-only section could not see). The last two clauses are the ones with teeth in the direction that decays: implementing an unmet gate, or adding one nothing wants, forces somebody to stand in front of the objective table rather than in front of this one. Asserted with positive controls over a constructed ledger, because a join over two regexes that quietly stopped matching would pass on two empty lists — the vacuity `GATE-TEST-7` was written against, in a test that reads two files instead of one. |
| `GATE-TESTGUARD-1` | P6 | held | A `ChangeSet` deleting a test, or adding `skip`/`xfail` to one, without a ticket is refused. (R1-QA-1, second half.) A rule about the *shape* of a change rather than its path, because tests must stay writable and no tier lists them. It is composed into `ChangeGuard.check()` rather than added as a second call each enforcement point would have to remember. Test files are recognised across Python, Go, TypeScript, Java, Ruby and Rust, and silencers likewise. `apply` passes a reader for the working tree so an *added* skip is told from one already there; with no reader the rule fails closed, because a false positive costs a ticket trailer and a false negative lets `fix` make CI green by silencing what failed. |
| `GATE-VERDICT-1` | none | held | The red and green verdicts are the framework's Test outcomes, `expect` is inverted in code, and a run that collected nothing or reported nothing is undecided. `PytestTest` read a missing summary line as zeros -- "a missing count is 0, not a crash" -- so a staged module calling `os._exit(0)` at import was a green suite of zero tests with `decided=True`, `TestVerdict.green` had no guard on the counts, and `implement/propose` marked the change ready on it; with `expect="fail"`, `os._exit(1)` read as red the same way (#313). Now a run with no summary line decides nothing on a clean exit and is `errored`, not red, on any other; a summary in which nothing passed or failed decides nothing either; `TestVerdict.green` requires a test to have passed, as a second lock on the verdict `implement/propose` reads back from an artifact it did not write; the tdd strategy's red and green phases both require a decided outcome, and its collection probe runs whenever the red phase did not decide; and `run_tests` refuses a path token beginning with a dash by name, the rule the worktree already applies to a ref, so the model cannot hand pytest an option. The positive control holds beside them: `3 passed` still decides green and `1 failed` still decides red. Asserted over the adapter with a stubbed runner and over the real pytest against a module that exits at import. Closed by #313. |

## Ledger, evals, metrics

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-LEDGER-1` | P4 | held | Every record written by `in_lockstep` carries `schema` and `epoch`; a record lacking them reads back as epoch `"ghaw"`. |
| `GATE-LEDGER-2` | P4 | unit only | `compare()` over records spanning two epochs raises `LedgerError`. **Nothing calls it.** Every `compare(` call site under `src/` is `receipt.compare`, an unrelated function about pack drift; the ledger's own `compare` is exported from `platform/ledger/__init__.py` and imported by nobody. `report` reads the ledger through `summarize`, and no command takes a before/after window, so no run has ever been refused for spanning an epoch change. The live sibling is what made this one easy to miss. **Decided with #163 (#266):** the loop's before/after arm does not go through `compare()`. It compares two answers to one promoted case, not two windows of run records, and forcing case results into record shape to reach the epoch refusal would be two writers of one comparison. The caller this function is for does not exist yet: a post-merge window comparison -- *did the runs after a merged proposal differ from the runs before it*, the §8.1 signals -- which is what would tell an adopter whether a proposal helped once merged. Still `unit only` until that exists, and O5 stays blocked on it, correctly: the loop can measure a draft and cannot yet measure whether a merged one helped. |
| `GATE-LEDGER-3` | P4 | held | A window whose records omit `credits` yields `mean_credits is None` and no credits delta. (Regression test for the fabricated −100%: `history.py` does `float(record.get("credits") or 0)`, coercing absent to `0.0`.) |
| `GATE-LEDGER-4` | P6 | held | Changing one guardrail file changes `EvalSubject` identity while `prompt_id@version` is unchanged. **Held since a run computes one.** `cli._eval_subject` builds the subject from the BOUND adapter's composition and `_write_ledger` carries its key, so every `review`, `triage` and `rfe` record says which runs it is comparable with and `report --by subject` slices on it. Built in `cli` rather than in the adapter that owns the composition because `evaluation` is a leaf that may import nothing of ours: the layer holding both the composition and the ledger is the one that can join them. Asserted over a record an actual `review --offline` run wrote, not a constructed one, and the guardrail half is asserted through the same join a run takes -- reducing the hashed text to the prompt body turns it red, which is the mistake a test calling `subject_for` directly would not have caught. `prompt_id` and `prompt_version` do not move, which is the point: identity is a content hash because editing a prompt without bumping a version is the normal way a prompt gets edited. |
| `GATE-LEDGER-5` | P4 | held | Every emitted metric name starts with `in_lockstep.` or `gen_ai.`, and none starts with the compiler's retired `lockstep.`. Checked from the **AST of every metric call site** as well as from a run, because a runtime check only sees metrics some test happened to trigger, and a metric added behind an unexercised branch is exactly the one that would carry a stale prefix. The wording is about a *prefix*: `in_lockstep.` contains `lockstep.`, so a substring check would reject every legitimate metric in the package. A test asserts that trap, so nobody later "tightens" the check into it. |
| `GATE-LEDGER-6` | P4 | held | The keys `credits`, `busy_seconds`, `pickup_seconds` appear in no emitted metric (name or **dimension**) and no current-epoch record. Both halves are asserted against real output rather than source text, and both were checked to fail when a key is reintroduced. Implementing it also collapsed two ledger writers into one: `cli._write_ledger` hand-rolled the record and stamped `schema` and `epoch` as literals beside the store that owns those constants, and a gate about what a record contains is worth only as much as the number of things that write one. |
| `GATE-LEDGER-7` | P4 | held | `priced_fraction` is `None` when no token was billable, and the metric and ledger field are **omitted** rather than defaulted. `1.0` for a run that spent nothing is coverage computed from an empty denominator, the same shape as a suite reporting 100% having judged nothing. The gate had no subject until the metric existed, and giving it one exposed what it measures: `price()` substituted the input rate for cache tokens whenever a rate declared none, silently, which is the fabrication this module's own docstring condemns one level up. The substitution stays, because it over-estimates and a ceiling that under-estimates is not a ceiling. The total is now labelled an upper bound by a number that says how much of it came from a declared rate. |
| `GATE-EVAL-1` | P6 | held | Editing a **skill body** changes `EvalSubject` identity. Held for the reason `GATE-LEDGER-4` gives directly above and over the same call site. **The parenthetical this row used to carry was wrong**: it said the gate guarded a progressive-disclosure hole -- §5.8 loading skill bodies on demand, so they are not in `composed_prompt_sha256` -- and in this codebase `PromptLayers.trailing_texts` inlines every skill body verbatim. So the composed hash moves on a skill edit too, and `skillset_hash` is belt and braces rather than the only thing holding this up. Both halves are asserted separately, because asserting only that the key moved passes on the separate hash alone: that is exactly what happened when the hashed text was reduced to the body, and it is how a claim about the composed text turned out to be checked by nothing. The separate hash stays -- progressive disclosure is a real design this framework may yet adopt for large skills, and on the day it does the identity should not quietly get worse. |
| `GATE-LEDGER-8` | none | held | A record on `lockstep-history` modified or deleted after its append is flagged: `GitLedger.verify()` returns one line per contradiction in the retained chain, `report` prints them at read time (stderr under `--format json`, so stdout stays parseable), and `doctor` raises `DOC167` as an ERROR so a required check fails on a rewritten ledger. Asserted in both directions: a clean append/reconcile/absorb history verifies empty, because a same-content re-add is the same blob and git records no change. **A reconcile or absorb never replaces a record: if a run id already exists on the destination with different content the operation is refused with `HistoryError` naming the run id, so the only way a record changes after its append is a rewrite somebody made on purpose** (tested in `test_gate_ledger_8_a_reconcile_refuses_when_a_run_id_collides_with_different_content` and `test_gate_ledger_8_absorbing_a_bundle_refuses_when_a_run_id_collides_with_different_content`). **Scope stated, not implied:** the check reads the retained chain only; a force-push that replaced the chain discards the contradiction with the commits that held it, so the remote half of the control is force-push/deletion protection on the history branch (`docs/controls-crosswalk.md`, unattended-run list). Post-1.0 addition (roadmap item 21), hence no phase. |
| `GATE-LEDGER-9` | none | held | A workflow record's `status` is a member of the status set, derived from its steps when the workflow returned no `Outcome` of its own (blocked, else errored, else failed, else succeeded; `decided` only if every step decided), and the record carries `steps` with each step's verb, status, reason and findings, so `history --explain` names the step that went red. The literal `"completed"` is gone: a status the set does not contain is a status `report` cannot count, and it counted eleven red selfchecks as 0% failed. `report` names schema-4 records that carry no verdict rather than folding them into "not failed". |
| `GATE-LEDGER-10` | none | held | A CI review's record reaches `lockstep-history`, or `report` says how many have not. Every pull request here pays for a review whose job holds the provider credential and `contents: read`, so its record could only leave as a bundle in an artifact -- and for two days of pull requests that is where every one stayed, on a 30-day clock, while the branch held eight paid runs (#294). Two halves close the pipe and a third counts what is left. `lockstep.yml` gained a `publish` job on the split `implement.yml`'s `propose` already uses -- `contents: write`, no provider credential, no provider SDK -- that downloads the review's artifact and runs `history --from-bundle --push`, under `always()` because a refused review is the run most worth a record of; the scaffold `init` writes gained the same job and the bundle step it needed. A scheduled `reconcile` workflow runs `history --from-artifacts lockstep-run --push`, which lists the artifacts the host still holds under that name and absorbs each outstanding one once: an artifact is outstanding when no record on the branch carries its run id (`ci_run`, schema 8) and no absorb commit names it, so a bundle from a run that recorded nothing is not downloaded twice, and an expired one is counted rather than skipped, because those records are gone. `report --scm` prints the same count -- `N outstanding, M expired before anyone absorbed them` -- and without `--scm`, or on a host that cannot list artifacts, a dash with the reason, because a branch presented as the whole record while fifty runs sit in artifacts is the reassuring figure this ledger exists to refuse. Asserted over both workflow files and the scaffold, the join in `outstanding`, the ledger's absorb notes, the command's output with the host stubbed, and `GitHubScm.run_artifacts` against the shape `gh api` returns. Closed by #294. "Once" was not true where the sweep runs until #307: a checkout has no local `lockstep-history`, and the ledger read that ref alone, so every artifact was outstanding every night and yesterday's absorb notes were never read. It holds in a fresh checkout now -- the read falls back to `origin/lockstep-history` (`GATE-LEDGER-12`), `reconcile.yml` checks out at full depth so that ref exists, and the test sweeps from two successive `git clone`s with a push between them, the second downloading nothing. The first dispatched sweep (run 34069508349, 2026-09-07) printed `artifacts 1 absorbed, 5 empty, 219 failed, 0 expired`: every absorb after the first was refused non-fast-forward, because the scratch ref a bundle is fetched onto was neither forced nor deleted and the next runner's orphan history does not descend from the last (#323). Both scratch refs are forced and deleted now, asserted over two bundles from two runners with nothing left under `refs/lockstep/`; the next dispatched sweep's line belongs here. **Hosts:** this repository's `lockstep.yml`, the GitHub trampoline `init` writes, and the GitLab one since #314 -- its review job bundled nothing and uploaded `.lockstep/` alone, #294's failure re-shipped to the other host while `docs/trampoline.md` said otherwise; it bundles now and a `publish` job holding the push token and no provider absorbs on every merge request, whatever review said. What the GitLab file cannot do is said in it: protected variables reach only protected refs, so the publish token is scoped unprotected to its environment with the file protected instead, or only protected-ref pipelines publish. No sweep on GitLab: job artifacts are not listed by name across pipelines, so `report --scm` prints a dash there. |
| `GATE-LEDGER-11` | none | held | A flagged rewrite can be acknowledged by name, and only then stops being an alarm. Every `report` on this repository opened with `TAMPERED` for four days because the 2026-09-02 reconcile carried a laptop's fixed-run-id record over the branch's own record of the same id -- exactly the event `GATE-LEDGER-8` exists to notice, and one nobody could answer, so the flag was read past and the next one would have been too (#295). The three outcomes the issue named were decided as the first: a recorded acknowledgement, not a re-append (a second record for the same run would be counted twice) and not an allow-list in `report` (a flag the reader silences). `history --acknowledge COMMIT --reason WHY [--by WHO]` appends `acknowledged/<commit>.json` to the branch -- who, why, when, and the lines the commit rewrote -- as a commit like any record; `verify()` returns the contradictions no note covers, `acknowledged_rewrites()` returns the rest with their note, and `report` and `doctor` print the note where the alarm was (`DOC173`, a NOTE) while an unexplained rewrite keeps `TAMPERED` and `DOC167` red. A note covers exactly the commit it names: a rewrite in any other commit is still an alarm, and the note itself is protected by the same walk, so editing or deleting one is a contradiction in its own right. Refused rather than written when it would say nothing checkable: no name or no reason, a commit not on the branch, a commit that rewrote nothing, or one already acknowledged. `--by` defaults to the configured git author whether or not the module opted into recording it, because running the command is the choice, and no name at all is refused rather than signed by the framework. Notes travel with the records through reconcile and absorb, so a note made on a laptop reaches the remote with the push that carries it. The acknowledgement of the 2026-09-02 rewrite is a person's act and is not made by the pull request that built the mechanism. Closed by #295. |
| `GATE-LEDGER-12` | none | held | The ledger is readable on a checkout that never wrote it, and a read creates nothing. `GitLedger.head()` resolved `refs/heads/lockstep-history` and nothing else, and a CI checkout and a fresh `git clone` create no local branch for a ref that is not HEAD -- so every reader of the ledger except the laptop that wrote it read nothing: the nightly sweep found every artifact outstanding, the learning loop found no trend in an empty census, and a second engineer's `report` said "no records yet" while the shared branch held thirty-four paid runs (#307). A read now falls back to `refs/remotes/<remote>/<branch>` when the local branch is absent, says which ref it read (`history` prints `read from`, `report` prints a `ledger` line), and leaves no ref behind: a read that created a branch would be a write nobody asked for. Writes on such a checkout create the local branch on the remote's commit in one compare-and-swap, so the first record lands beside the branch's and not on a second orphan history. `history --pull` is the act that brings the remote's records onto the local branch, record by record through the same fold `absorb` uses, and pushes nothing; `report` never pulls, and instead counts the records each ref holds that the other does not, in both directions, rendering a dash where no remote copy was fetched rather than "0 behind". The jobs that read the ledger, `reconcile.yml` and `improve.yml`'s measuring job, check out at full depth so the remote copy is there to read. Asserted over a bare origin and a real `git clone`: the clone reads the writer's record with no local ref before or after, the first write's parent is the remote's commit, `pull` ends with both sides' records and `verify()` empty and the origin untouched, `improve_measure` over remote-only records prints the same trend line as over local ones and refuses `improve.no_trend` once the remote copy is deleted, and the two commands on the clone show the run and name the ref. Closed by #307. |

## Outcome and ledger store

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-OUT-1` | P1 | held | `Status` has exactly 5 members and `UNDECIDED` is not among them. Six until #267: `SKIPPED` was reserved to a `Cache` middleware that was never written, so it was a member of a closed taxonomy nothing produced, and it went with the two `GATE-OUT-2` clauses that described it. `PARKED` was reserved at 1.0 with no producer by a recorded deferral (§17.11) rather than a comment, which is the difference between a member a decision reserved and a member a comment reserved; it has producers since Phase 5 -- `ctx.park` and a `fan_out` with a human branch (`GATE-OUT-6`) -- and the enum did not have to grow to get them, which was the point of reserving it. |
| `GATE-OUT-2` | P6 | held | An unjudged rubric yields `Outcome(SUCCEEDED, decided=False)` with `pass_rate None`, and both the ledger and the `in_lockstep.action.outcome` metric carry `decided` apart from `status`, so an undecided success is never read as a pass. ~~A cache hit yields `Outcome(SKIPPED, decided=True)`; `Cache` refuses to store `decided=False`.~~ **Struck by #267.** Two of this row's three clauses described a middleware nobody wrote: `Outcome.skipped()` had no caller under `src/`, no run had ever produced a `SKIPPED`, and `middleware/` held no `Cache`, so the ledger and the metric were being credited with telling apart two things one of which did not exist. The issue posed writing the middleware or striking the clauses, and striking was the smaller and the right one -- nothing asked for a cache, and a gate is a claim about what is enforced, not what a design once listed. `Status.SKIPPED` went with the clauses (`GATE-OUT-1` counts five now), because a member of a closed taxonomy that nothing produces was itself the reservation with no mechanism behind it. What holds is the clause this project keeps citing, on a live path: `pass_rate` is `None` rather than `1.0` when nothing was decided (`evaluation/cases.py`, `trial.py`, read by `report` and `pack try`), `decided=False` is produced by a bound adapter (`pytest_adapter.py`) and by every `blocked_by`, and the ledger record and the outcome metric each carry `decided` as its own field. |
| `GATE-OUT-3` | P10 | held | `JoinResult.decided == all(branch.decided)`. Off the §17.11 cut line with `GATE-ASYNC-3b`. `JoinResult` is a frozen mapping of branch name to `Outcome`; its `status` is the precedence a run's steps already use (`VERDICT_PRECEDENCE`, moved to `outcome.py` so the run and its branches cannot come to read a mix two ways), its `cost` the sum, its `reason` the deciding branch's, and `as_outcome()` carries every branch's findings and the join itself as value. One branch that judged nothing is a join that judged nothing, whatever the other three found -- which is why `decided` is a field on `Outcome` and not a status member (`GATE-OUT-1`). Each branch is recorded as a step of the parent run, named for the branch, and ran under its own scope path, so a checkpoint or a cassette for `security` cannot be confused with one for `tests`. `resume=` is refused by name until a branch can park (`GATE-OUT-6`). |
| `GATE-OUT-4` | P4 | held | `LedgerStore` declares `compare_and_set`; the shipped in-repo store implements it and reports `scope == LOCAL`. **The SHARED half, off the §17.11 cut line (Phase 5):** `GitLedger(shared=True)` reports `SHARED` and provides it as `git push <remote> <blob>:refs/lockstep/state/<key> --force-with-lease=<ref>:<expected blob>` -- the remote updates the ref only if it still points where the lease says, under its own lock, so the ledger's remote is the coordinator and nothing of ours sits between eight runners. Values are blobs, so callers speak in values and never see a sha; `expected=None` is create-if-absent. Asserted over eight clones of one bare origin claiming one key at once: exactly one success, a later swap with the winner's value succeeds, the same swap again is stale, create-if-absent fails once the key exists, every clone reads what the remote holds, and no clone is left a local ref. The default construction is the LOCAL store it always was and refuses naming the constructor. One `LedgerScope`, on the port; the copy `store.py` carried is gone. `RunContext.ledger` carries the store as the port, resolved by the decision the record writer already makes, which is what a parked branch will write through (`GATE-OUT-5`, `GATE-OUT-6`). |
| `GATE-OUT-5` | P10 | held | `ctx.fan_out` with a `ctx.human()` branch on a `LOCAL`-scope store raises at the call site, before any branch starts. Off the §17.11 cut line (Phase 5, PR-12). `LocalStoreCannotPark` names the store and its scope and the construction that would fix it, and it is raised rather than returned because it is an error about the lifecycle's bindings, not an outcome of the run: a barrier on a ref one machine can see is one nobody on another machine could complete, and finding that out after three machine branches have spent is finding it out too late. Asserted with a counting adapter whose call count stays zero and a step list that stays empty, over a context with no store and one with a LOCAL one. |
| `GATE-OUT-6` | P10 | held | `ctx.park` on a `LOCAL` store returns `BLOCKED` naming the store; on `GitLedger`, 8 concurrent barrier ticks launch the continuation exactly once. Off the §17.11 cut line (Phase 5, PR-12). A park writes the barrier record -- boundary, continuation id, head, payload, every branch's state -- into the shared store under `barrier/<run id>` as a create-if-absent, ends the run `PARKED` (a step, so the record derives `parked`; `PARKED` now leads the verdict precedence because a run waiting on a person has not ended), the process exits `EXIT_PARKED` printing the resume command, and the host carries the `lockstep:parked` label and the park's JSON in a sticky comment on a `pr_review` boundary. On a LOCAL store, or none, `BLOCKED`/`park.local_store` with a finding naming the store and `GitLedger(shared=True)`. The tick (`platform/barrier.py`) reads the record, applies the event through `core.human.apply_event` -- deduped on `(run, branch, event id)`, the branch `parked -> resumed` carrying the verdict as its status -- and swaps the new text against the text it read; a lost swap re-reads with jitter, and the write that makes every branch terminal launches the continuation as a fresh run carrying `parent_run_id` and a `Resumption` found by annotation. Asserted over eight `GitLedger(shared=True)` clones of one bare origin ticking the same event at once: one launch, seven losers that re-read and found it applied. **With a negative control** the way `GATE-ASYNC-4` keeps one: the same ticks with the swap awaited on the loop run one after another, which the timing assertion detects. The control found a real defect on its first run: a push whose blob the ref already held exited 0 having updated nothing, and eight serialised ticks all "won" -- the swap reads `--porcelain`'s flag now, and `=` is somebody else's write. End to end at the command line: `run` parks and exits 4, `ls --parked` lists what it waits for, `resume --run --as --by` runs the continuation, the child's record names its parent, and the same event twice is a duplicate. What is not built, and said: the webhook trampoline and the design's notifications section (resume is a command), `sweep`'s expiry and escalation, `Resumption.staleness` (always None: absent is not zero). |
| `GATE-OUT-7` | P10 | held | An all-machine `fan_out` on the default store performs zero ledger writes. Off the §17.11 cut line with `GATE-ASYNC-3b`. The join is returned inline and the run continues; the run's one record is written by the run, after it, as ever, and the barrier record is for a branch that parks, which nothing here can (`GATE-OUT-6`). Asserted with a counting `LedgerStore` bound in the container across a three-branch join: zero writes. Small today, and kept because it is the row `GATE-OUT-6` will have to argue with: the day a human branch writes a barrier record, this row says the all-machine join still must not. |

## Security

The first revision of the plan had **none** of these. The plan's own thesis, "a layering rule with
no enforcement will not survive seven phases", applies with equal force to security rules.

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-CFG-1` | P2 | held | A `lockstep.py` modified in the head tree has **zero** effect on the resolved container; config resolves from the trusted ref. Run as a fork-simulation fixture, **and against the checkout shape CI actually produces**: a detached HEAD with `origin/main` and no local `main`, where `git show main:lockstep.py` fails for a reason that has nothing to do with the file. That case made this control silently inapplicable for a real CI run. It returned `None`, indistinguishable from "this repository has no configuration", and fell through to detected defaults. An unresolvable ref is now a refusal, and the CLI prints which ref configuration came from, because a provenance control whose output nobody can see is one whose absence nobody notices. |
| `GATE-CFG-2` | P3 | held | `doctor` fails when config would resolve from the ref under review. |
| `GATE-CFG-3` | none | held | Every Python file in this repository is reached by the tools `make check` runs, or is exempt with a reason somebody wrote down. `ruff check src tests` and `mypy src` left `.lockstep/lockstep.py` in neither -- 771 lines binding every adapter, the path tiers and the egress policy. Pointing mypy at it named a function defined twice in about a second; ruff could not, and was right not to, because the first copy is called before the second is defined so `F811` correctly stays silent. `.lockstep` is named EXPLICITLY in each target and a test asserts that it is: neither tool descends into a dot-directory when walking, so a parent path would have looked like coverage and opened nothing -- the gate's own failure reintroduced by its fix. Asserted as a ratchet over the whole tree rather than as a check on one file, because the gap reopens wherever the next unchecked file lands: the test reads the paths off the Makefile and fails on any `.py` outside them. Exemptions are per tool and carry their reason: `tests` for mypy (693 errors, its own project, #248). `examples` was exempt for both tools until #247 -- two files named `lockstep.py` cannot share a mypy invocation, so each worked example now gets one of its own, the wayfinder one naming the modules beside its lifecycle file, and the ruff lines name each example's `.lockstep`; the seven type errors the four examples carried, on the files an adopter copies, went with the exemption. `tests` was the other mypy exemption -- "693 errors, its own project" -- and #248 removed it: 831 errors by the time it was picked up, worked to none under `strict` less `disallow_untyped_defs`, with a `tests/__init__.py` so the `tests.*` override in `pyproject.toml` names modules that exist -- it never had, and nothing had run it. What the pass found in `src` is written on the seams it touched: a `Runner` and an `Invoker` protocol where adapters had named the shipped class and refused their own opt-out, and a `Container.bind` whose `T` held for ports and not for the request-to-adapter binding every lifecycle file makes. Both exemption maps are empty now. Filed as #235. |
| `GATE-EGRESS-1` | P3 | held | A `ContextPackage` containing any `UNTRUSTED_EXTERNAL` item with `EgressMode.NONE` yields `BLOCKED` before the first model call. |
| `GATE-EGRESS-2` | P3 | held | `ENFORCED_*` performs a live probe to a known-blocked host and refuses to start if the probe succeeds (verified, not attested). |
| `GATE-EGRESS-3` | P3 | held | An MCP tool with undeclared capability is treated as `REACHES_NETWORK`, not read-only. |
| `GATE-RESIDENCY-1` | none | held | On a restricted repository (`IN_LOCKSTEP_RESTRICTED`, or a bound `EgressPolicy(restricted_repo=True)`), a model whose registration is not `DataPolicy.INTERNAL` yields `BLOCKED` (`residency.external_model`) before the first call, including a hand-built invoker that declared no policy, which fails closed as undeclared. Not lifted by `UnsandboxedEgress` (the egress opt-out says "my network is my business", not "this repository is unrestricted"); lifted only by `transmits=False`, the same cassette/dry-run exemption egress grants, because residency is about where bytes land and a replay sends none. This gate is why `registry.data_policy_for` exists: until it had this caller, `data_policy` was a stored opinion, and `restricted_repo` was a parameter nothing set. A registration that could not state its destination (`endpoint=None`, with the reason `register` requires) is refused on a restricted repository by that reason (`residency.unknown_destination`) whatever its data policy says, because a policy about where the bytes land cannot be checked against a host nobody can name (#309). Post-1.0 addition (roadmap item 20), hence no phase. |
| `GATE-GUARD-1` | P2 | held | A synthetic write to each Tier-1 path is refused on **all three** paths: the in-loop tool boundary (`Workspace.record`, where a refusal is a tool result so the model can choose again within its budget -- `write_file` and `edit_file` both stage through it, the edit reading through `check_read` first), `apply-inline`, and `apply --from-artifact`. All three are driven from **one** list of Tier-1 paths, so a rule holding in one place and not another cannot pass. The two missing points existed as places rather than things until the builtin tool runner did: `AiInvoker` accepted `tools` and `run_tool` and every shipped verb passed neither, and there was no `apply-inline` at all. The two apply paths make the identical `_guard_or_exit` call, because the way a set of enforcement points goes wrong is that one grows its own slightly different check. |
| `GATE-GUARD-2` | P3 | held | A `ChangeSet` whose `FileChange` resolves outside the repo root, or introduces a symlink that would, is refused. The check runs on the **post-change tree**. |
| `GATE-GUARD-4` | none | held | What a model may READ is refused at every tool that reads: `read_file`, `search_text` and `list_files`. `_read` consulted `ChangeGuard` and acted on one of its answers — every tier-1 refusal but `outside-repo-root` was computed and thrown away, so `read_file(".env")` returned the file, the guard consulted and overruled in the same expression. `search_text` was worse and is the half that matters: it walked `rglob("*")` with no guard at all, and a search is how somebody looks for a credential rather than how they open one they already know about. The read list is deliberately SHORTER than the write tiers and `DENY_READ` says why: `.github/` is tier 1 for writes because editing it changes what CI runs, and reading it is how an agent asked to fix a workflow finds out what the workflow does — so reusing the write list would refuse reads nobody needed refused, and a guard that says no to ordinary work is one somebody turns off. What is refused is the paths whose CONTENTS are the risk: `.env*`, private-key material, `.git/` (every version of everything, including what a later commit removed), and `.lockstep/cassettes/` and `transcripts/` — a recording read back is the next run's prompt, a concentration of everything the last session touched arriving as context nobody chose to supply. Asserted in both directions, because a guard that refused everything would be the same defect wearing the other coat. Extensible on `PathPolicy` like the other two lists (O8). |
| `GATE-GUARD-3` | P6 | held | A strategy selected from ticket-label input cannot hold the `prompts/**` grant. |
| `GATE-REDACT-1` | P2 | held | Every module is AST-scanned for a raw write primitive (`write`, `write_text`, `write_bytes`, `writelines`, `open`/`fdopen` in a write mode); each use must be inside `privileged/sink.py` or carry a written reason it is not a redaction sink. Stdout and stderr are covered by wrapping the *stream* at CLI entry rather than the sixty-odd `echo` calls, so a call added later is covered without anyone remembering. A companion test fails on an exemption whose call site is gone, so a licence cannot outlive its subject. |
| `GATE-REDACT-2` | P2 | held | A secret framed as base64 and as `Authorization: Bearer <v>` is redacted in stdout, span attributes, cassette, artifact, and ledger. Not just the ledger. |
| `GATE-REDACT-3` | none | held | A write does not carry a mask a read put there. The model reads through the redacting sink, so a key-shaped string in a file reaches it as `***`; a model that edits one line and writes the file back was writing the mask over the value, and this repository's first `/fix` on itself did exactly that to a test's fake credential and failed the suite it had otherwise fixed (run 34129809277, #312). `Workspace.record` matches every mask in the staged contents to the value the file holds at that position -- the file's own redaction says where those are -- and puts the values back, telling the model how many; a count that does not match is refused, because then a mask is one the model typed and nothing guesses which. The value goes back into the STAGED change only, which reaches the disk through `apply` and a person; it never reaches the model, the tape or the ledger, which is the property `GATE-REDACT-1` and `-2` hold and this one has to keep. |
| `GATE-AUTH-1` | P2 | held | Every provider **and every MCP server** constructs with `os.environ` monkeypatched empty. |
| `GATE-AUTH-2` | P2 | held | A constructed client's base URL equals its registered `endpoint`, else refuse -- and the comparison is real for every shipped registration. It was not (#309): `anthropic` registered `base_url=""`, so the SDK read `ANTHROPIC_BASE_URL` itself and dialled a proxy while the registration, residency and `egress-manifest` said `api.anthropic.com`; `bedrock`, `vertex` and `gemini` registered `endpoint=""`, and the check skipped an empty side. Now the base URL is resolved in `bootstrap`, where the environment is already read (GATE-AUTH-1 kept), and handed to the settings, so a proxy set through the environment is refused at `provider_for` naming both hosts; the Claude transports report the URL off the constructed client, so Bedrock's and Vertex's region-derived hosts are compared with what the SDK built; the cloud registrations derive their endpoint from the region and, without one, register `endpoint=None` with the reason, which `register` requires (an empty string is refused outright). Gemini's host is declared from the same region and reported back by the provider, not read off google-genai's client, which does not expose it. Left uncompared by design: a custom provider whose `base_url()` is the interface default `""`. Asserted over the shipped registry with the variable set and unset, Bedrock constructed for a region, the three cloud registrations without one, and the refusals at `register`. |
| `GATE-SANDBOX-1` | P3 | held | `Test`/`Validate` run out-of-process; a `conftest.py` attempting to read a `Credentials` object from the parent process cannot reach one. |
| `GATE-SANDBOX-2` | none | held | A file the MODEL staged runs only in a container, or the run refuses by name. GATE-SANDBOX-1 is about the repository's own suite and the parent process's memory; this is about the test file a model wrote on a ticket that is untrusted by construction, which `run_tests`, TDD's red and green runs, `DiagnoseThenFix`'s reproducer and the workflow's verdict over a staged change all materialised into a worktree and handed to whatever `Test` was bound -- and `Sandbox()` with no image, the runner `init` scaffolded and this repository bound, ran it as a subprocess on the host with `HOME` and an open socket: `~/.ssh` readable, and on a runner the token `actions/checkout` persists into `.git/config` one `git config` away through the linked worktree's `.git` file (#308). Three clauses. **Refused by name:** every one of those four sites asks `staged_refusal` BEFORE a worktree exists (the strategies before their first model call, so nothing is spent on a test that would then be refused a runner), and a runner that would put the files on this host -- no sandbox, `UnsandboxedRun`, no image, or an image without `require_container` on a machine with no runtime, the one case probed rather than declared -- is `blocked` with `sandbox.host_fallback` naming the line to add; `PytestTest` maps a `refused:no-container` at run time to `blocked` as `CommandTest` has since #256. **Contained:** under the shipped flags the container has `--network=none`, `--init` (so a process a staged test starts and kills is reaped rather than left a zombie that reads as alive), the throwaway tree as its only writable mount -- writable in fact, because the container runs as the host user (`--user`, and `--userns=keep-id` on podman): with every capability dropped, root inside could write only what "others" may, and on a runner the tree is uid 1001's at mode 755, which is how a fourth `/fix` failed on ruff's cache directory (#312) -- every `mounts=` entry read-only, and neither the host's `HOME` nor its user name reaches it (`/tmp` and `sandbox` inside, the name because a uid the image's passwd file lacks is a KeyError to `getpass.getuser()`); asserted on the argv always, and live -- a staged probe that cannot see a file beside its tree and whose `connect()` fails -- where the image is already pulled, skipped by name elsewhere, which is the honest state of a test that would otherwise pull. **Not persisted:** every checkout in a job that can hold a provider credential, in this repository's workflows and in every trampoline `init` writes, sets `persist-credentials: false`; the credential-split test that was meant to see those jobs decided `spends` from a string every workflow had moved into a comment, was true of no job, and now reads `id-token: write` and provider keys under `env` with a fixture it fails on. **The boundary, stated because it is real:** the repository's own suite run by a person (`run selfcheck`) keeps the credential-dropped subprocess, which is GATE-SANDBOX-1's claim and not this one's; a container needs an image that carries the suite's dependencies, which no stack image does because the worktree is a copy of HEAD without `.venv` or `node_modules`, so `init` writes the rebind as a commented line and names it in the things-to-decide list rather than inventing one, and an adopter's model-staged run refuses until a person names it; this repository mounts the `.venv` its `Provision` built read-only into the base interpreter's image, which runs its suite on the linux runner where its write verbs run and on a laptop for as long as every package the suite imports is pure Python (its are; `run selfcheck --paths tests/in_lockstep/test_verdict.py` ran 138 tests under `podman:docker.io/library/python:3.13` on a macOS venv; the image is uv's `python<version>-bookworm` since #312's first run, because the suite needs `git` and `uv` of its environment and the slim and full Python images lack one each), and would fail at import, naming the module, on a compiled dependency built for another platform. |
| `GATE-TOOLING-1` | none | held | The deterministic adapters run the repository's tooling, never the interpreter running `in-lockstep`: `PytestTest` resolves the repository's `.venv` first, then this process only when it lives inside the repository, then PATH; `RuffValidate` resolves ruff the same way, beside that interpreter before PATH. A tool found nowhere is a refusal naming every place looked, not a red suite, and pytest missing from the resolved interpreter is an error, not a failure. `ls` prints each binding's resolution, and for a loaded lifecycle module `doctor` raises `DOC180`/`DOC181` when a bound tool is absent or cannot be run from where it was found (a repository with no module runs on detected defaults, whose resolutions `ls` shows). Two first-time installs hit the old behaviour on their first `selfcheck` (#167). |
| `GATE-TOOLING-2` | none | held | A repository states how it builds itself in a file detection reads, or detection declines by name. `_detect_facts` reads `pyproject.toml`, `package.json`, the `Makefile`, `Cargo.toml`, `go.mod`, `pom.xml` and `build.gradle[.kts]`, and derives only what a toolchain guarantees for any valid manifest: `cargo test`/`cargo build`, `go test ./...`/`go build ./...`, and the JVM commands **only** where `./mvnw` or `./gradlew` is on disk, because `mvn` and `gradle` are not guaranteed to be installed. What is deliberately not derived: `cargo run` and `go run` without a main (a library crate fails at run time, the wrong default that runs); and any linter the repository did not configure — `go vet` would run for any module, so binding it guesses that this repository treats it as its linter. A written `test` target or `"test"` script beats a derived command, because each is a decision and the derived one is inferred from a file's presence. `RepoFacts.stack` names every ecosystem present rather than the first matched. The decline is `RepoFacts.declined()`, printed by `ls` and `init`, and it distinguishes three cases: a tree with no manifest is told every file that was looked for; a `pom.xml` with no wrapper is told about the wrapper, because that repository *did* say how it builds; a Makefile with no usable target is told so. `test_detect.py` names this gate, and holds `BUILD_MANIFESTS` to being true — every advertised file, alone in a directory, must produce a fact. What is still unread is `GATE-TOOLING-3`. Closed by #237. Since #316 a written `"lint"` script is reused before an eslint config is inferred, the same precedence `test`, `build` and `start` have; the decline says detection reads the repository root and names any manifest found one level down, so a monorepo is told what it is rather than "unsupported"; `init` refuses outside the root; and the scaffolds decline where detection declined -- the write-verb blocks write the same commented Test stub plain `init` writes instead of binding `PytestTest()` for a stack detection could not place, the sandbox image is derived from the one detected stack or left as a stub naming the Dockerfile or the second stack, the pytest line is generated from the constant `detected_bindings` installs, and each trampoline installs the extra and guards on the secret of the provider the module routes the verb to, refusing by name a provider `init` has no recipe for. |
| `GATE-TOOLING-3` | none | unmet | A repository whose build is declared in a manifest `GATE-TOOLING-2` does not read still has its bindings hand-written. `Gemfile`/`Rakefile`, `composer.json`, `mix.exs`, `*.csproj`/`*.sln`, `CMakeLists.txt`, `Package.swift` and `BUILD.bazel` are each a repository saying how it tests and builds itself, and each is declined by name rather than read. That decline satisfies O1, which sanctions declining in its own sentence — *detection that guesses is worse than detection that declines*. It does not satisfy O2, whose standard is that what a person writes by hand is the thing nobody could have discovered for them: a Ruby team writes `CommandTest(["bundle", "exec", "rake", "test"])` out of a Rakefile that was sitting in the tree. That is the whole distinction between the two rows this gate now separates, and it is why closing `GATE-TOOLING-2` moved O1 and left O2 where it was. |
| `GATE-PROVISION-1` | none | held | A scaffolded work job can run the repository's suite: `in-lockstep provision` builds the environment `GATE-TOOLING-1` resolves first, from a file that exists (`uv.lock`, `requirements.txt`, `package-lock.json`, a Makefile `deps` target) and never from a guess; a pyproject without a lock, and a Poetry, PDM or Pipfile lock, bind no Python provisioner. The steps run in order and stop at the first failure; nothing to provision is `not bound` and exit 0, never a success; a provisioner found nowhere is a refusal naming every place looked; a containerized sandbox that denies the network is `blocked`. It runs in the work jobs only, before `doctor`, with no credential in its step (on GitLab, in the job; on both hosts the sandbox's allowlist keeps the key out of an install hook) and without `continue-on-error`: never in review (its checkout is the change under review) and never in propose (whose commit sweeps its tree). The trampoline stays byte-identical across repositories (#185). Since #316 every lockfile binds its own tool's frozen install -- `poetry install`, `pdm sync`, `pipenv sync`, `yarn install --frozen-lockfile`, `pnpm install --frozen-lockfile` beside `uv sync --locked` and `npm ci` -- and whether the tool is installed is `ls`'s resolution line and DOC180, the answer `uv` and `npm` already got; a pyproject with no lock at all still binds nothing. |
| `GATE-PROVISION-2` | none | held | A repository `init` scaffolds reaches a green run with no hand-editing. `init --implement` binds `ApprovalGate` and `selfcheck` dispatches Test, which declares `EXECUTES_CODE`, so the gate blocks -- correctly -- and what must hold is that the run is reachable by the flags the refusal names. Both forms are asserted: `--approve` for a person watching, and `--approved-by` for the unattended trigger. Asserted as a verdict rather than as the absence of the refusal, because a run that dispatched nothing at all would also not print it (#189). The row is new; the assertion is not, and writing it down is what lets O2 cite something for the claim that onboarding is light. |
| `GATE-APPROVAL-1` | P3 | held | An adapter that lets a **model** write or execute, with no approval path in the middleware chain, is refused at startup rather than at call time. **Narrowed from the literal wording, deliberately:** read as "any adapter granting `WRITES_FILES`/`EXECUTES_CODE`", it refuses every repository that runs its own test suite, since `PytestTest` declares `EXECUTES_CODE` and means it. `Sandbox` is the control for a deterministic adapter that executes code; approval is the control for one that *also* spends money, because there a model is choosing. Approval is a **declared** property (`provides_approval`), not a class identity, so a house gate routing approvals through an organisation's own system of record satisfies it. Fires for nothing shipped (`AiReview` is read-only), which is the right answer for a framework with no write verb. |
| `GATE-POLICY-1` | P1 | held | `PolicyStack` merges monotonically: ceilings take the lowest not the last, scan is strictest-wins, deny-tools union — so a later contribution can only ever tighten an earlier one. **The resolved result reaches the loop.** `InvokePolicy.under()` composes an adapter's own needs with the stack: `max_turns` tightens and never raises, `deny_tools` is removed from the `ToolSet` so a denied tool cannot be dispatched rather than being refused when called, and `scan_input="block"` refuses before the first model call where `warn` records and proceeds. Until that, `resolve()` had exactly one caller (`ls`, printing a summary), so a contributed ceiling was a comment. Narrowed by #263, which deleted `network`: *deny-all is an irreversible floor* was this row's clearest clause and its only one over a field nothing enforced. The property is not lost with the field — a merge that only ever tightens is what `scan_input` and `deny_tools` do, on values the loop reads — and the row no longer claims it of a string nobody consulted. |
| `GATE-CI-1` | P7 | held | No workflow under `.github/workflows/` invokes `lockstep`; no `aw-*.lock.yml` remains. |
| `GATE-CI-2` | none | held | Every `in-lockstep run <family>/<step>` a GitHub trampoline invokes has a GitLab job that invokes it, and the reverse, walked from the files rather than from a list in the test -- and the walk fails on a fixture that adds a step to one side (#314). The GitLab file `init` writes carries the four verbs GitHub's do: `implement/from-ticket` and `implement/propose`, `fix/from-ticket` and `fix/propose` on one gate/work/propose pair with the verb a pipeline variable (`LOCKSTEP_VERB`, `implement` unless the pipeline says `fix`; one pair because the trigger is the same run-pipeline-with-variables either way, so a second copy would be one more place credential scoping has to be right), and `improve/measure` and `improve/propose` on a measure/improve-propose pair under `LOCKSTEP_IMPROVE`, which a pipeline schedule sets. Every job is active and held inert by a `rules:` clause pinning it to the default branch, so a repository that never asks never runs them. `init --fix` there names the jobs and the variable that run fix; it used to say the jobs were "already in .gitlab-ci.yml" while the file ran implement and nothing else. `init --host github|gitlab` says which trampoline where the remote and the CI files do not: a remote naming neither host gets no trampoline and the two flags by name, where it used to get a GitHub workflow its host ignores; a repository with no remote at all keeps the GitHub default, said out loud. The credential split is asserted on GitLab's own spelling: `work` may reach a model and carries no push URL, `propose` may push and holds no provider credential, `gate` holds neither. Keyless CI is `id_tokens:` minting a JWT into `ANTHROPIC_IDENTITY_TOKEN`, which the framework hands straight to the SDK's own jwt-bearer chain, and the audience is asserted equal to `ANTHROPIC_FEDERATION_AUDIENCE` rather than written twice. **What the walk does not cover, said here:** the learning loop's GitHub spelling is this repository's own `improve.yml`, which no scaffold writes, so an adopter on GitHub hand-writes what an adopter on GitLab is given; the work jobs' image carries Python and uv, and a Node, Go, Rust or JVM repository is told to name one that carries both, because none on a registry does. **Never executed.** This repository is GitHub-hosted, so no GitLab pipeline has ever run these jobs and no federation exchange for a GitLab issuer has been performed; whether a rule can be created for that issuer at all is a question for the provider. Held on the same terms as `GATE-RECORD-1` -- assertions over the scaffold an adopter is actually given -- and README's GitLab row stays `partial` by decision until an instance runs it. Closed by #236; rewritten to what the test proves by #314. |
| `GATE-CI-4` | none | held | A lifecycle workflow invokes the framework and decides nothing. Every shell statement in every lifecycle workflow this repository ships (`implement.yml`, `fix.yml`, `ai-generated.yml`, `lockstep.yml`, `improve.yml`, `review.yml`; `ci.yml` and `release-python.yml` are this repository's own build and release) matches an allowlist of framework invocations, and each file has a statement cap that is raised with an argument. The rule existed for `implement.yml` alone (#314): `lockstep.yml` carried `status=0; for aspect in …; do … || status=$?; done; exit $status`, a loop over the lenses with the worst-status rule inside it, and `improve.yml` carried `test "$status" -eq 3 && exit 0`, the decision that a refused scheduled run is green, plus a `hashFiles` guard deciding whether to propose -- three pieces of lifecycle logic in files nothing tests, and a decision `improve_propose` already makes in Python made a second time in YAML. Each moved into the framework where a test reaches it: `review --aspect` repeats, runs every lens named as its own run with its own record and its own routed model, and exits with the worst status (a failure outranks a refusal); `run --blocked-ok` exits 0 on a blocked run and says so in the log, so a scheduled job whose refusal is the control working is green with a reason rather than a shell translation; the guard is gone because `improve_propose` refuses `improve.no_proposal` by name. The list of lifecycle workflows is itself asserted against the directory, so a workflow added later is under the cap or the build says so. |
| `GATE-CI-3` | none | held | A verb this repository tells adopters to run in CI is one this repository runs, and a verdict it prints is one something acts on. Both hold now, and the second was the harder half because it was not a policy question. `doctor` was `continue-on-error: true` at all four call sites, and it had to be: `_branch_protection` reported **every** unreadable answer as `the default branch has no protection rule`, at ERROR, and a job token cannot read that API — so a fully protected `main` was reported unprotected on every run and `doctor` exited 1 every time. The verdict was discarded because the diagnostic cried wolf. Fixed where it was wrong: only GitHub actually saying `Branch not protected` is `DOC121`/ERROR, anything else is `DOC120`/NOTE quoting what `gh` said, which is what its neighbours `DOC122` and `DOC125` already did — *absent is not zero*, inside the tool that checks for absent controls. The default branch is asked for rather than assumed, so a repository whose default is `master` is no longer told it has no rule. With that, the four sites drop `continue-on-error` and gain the spend-limit attestation they were being marked down for not seeing. On the first half: `Provision` is bound in `.lockstep/lockstep.py` (an explicit module wins over detection, so a module binding Test and Validate and staying silent left the verb reporting `not bound`) and the work jobs run `provision` before `doctor`, in the README's own order. One limit, measured rather than assumed and recorded in the workflow files: `uv sync` still bootstraps, because this repository runs the framework *in the tree* and something must build that environment before `in-lockstep` exists to run — so the step proves the binding, the tool resolution and the sandbox against this repository, and cannot prove a stale lockfile fails, since `uv run` has already refreshed one by then. Closed by #249. Since #316 the scaffolds hold the same two properties this repository's workflows do: no `run` in a trampoline states `--budget`, because the module declares the ceiling and a flag would replace it, and no `doctor` step is `continue-on-error` or `|| true` -- it reads `IN_LOCKSTEP_ORG_SPEND_LIMIT` from a repository variable the post-init list names, and is red without it on purpose. |
| `GATE-PLUGIN-1` | none | held | The container docstring's promised order (explicit, then plugins, then defaults) is real end to end: an `in_lockstep.standards` entry point applies during `Lockstep.detect()`, its every bind lands at `Tier.PLUGIN` (the facade offers no tier parameter, so a plugin *cannot* bind `EXPLICIT`), its policy layers carry `plugin:<name>` as source, application order is entry-point-name order on every machine, and the repository's own `lockstep.bind` wins whether it ran before or after. A plugin that fails to load raises `StandardsError` naming it rather than running without standards somebody installed; there is no skip-loading environment variable, per the sealed-standards posture (visibility of removal, not impossibility). `ls` prints what applied. Post-1.0 addition (roadmap item 23), hence no phase. |
| `GATE-PLUGIN-2` | none | held | An adopter's own `lockstep.py` type-checks against the installed distribution, and everything `init` writes passes. Two halves, and shipping the first without the second would have been worse than shipping neither -- an adopter meets the errors on their first run and concludes the framework does not type-check. `py.typed` travels in the wheel beside `__init__.py`, asserted against the imported package rather than the source path because those are the same directory for an editable install and different ones for a wheel, which is the case that matters. And every scaffold `init` can write survives `mypy --strict` run from a directory with no config of its own, so the repository's own `[tool.mypy]` cannot supply a setting an adopter would not have. The second half is what found the defects: `-> Outcome` is `type-arg` because `Outcome` is `Generic[ValueT]`, and `init --implement --fix` wrote `_last_unsuccessful` twice, the second shadowing the first, in a file `init` had just generated. That was #190's other half -- it de-duplicated imports and not definitions -- and the de-duplication is byte-identical definitions only, because two functions of one name whose bodies differ are a real conflict and the honest response is to leave both and let the adopter's checker say so. Parametrised over each flag combination, because each verb alone was clean and only the combination was not. Filed as #232. |
| `GATE-PLUGIN-3` | none | held | What `init` writes REFERENCES the shipped processes; it does not copy them. `init --implement --fix` used to append ~560 lines of workflow source into the adopter's module, and the copy was the defect three ways over: every fix landed twice, a fix could never reach a tree that had already run `init`, and the two copies had drifted **96 lines of real code** apart by the time anybody diffed them -- with only this repository's under test. The bodies live in `in_lockstep/workflows/` now and reach the RUN CONTEXT rather than a module-level `lockstep`: `ctx.container`, `ctx.repo.root` and `ctx.max_attempts`, the last snapshotted onto `RunContext` beside `models` for the same reason. Registration is a call, never an import side effect, because `@workflow` raises `DuplicateWorkflow` on a repeated id -- a module still carrying its own ejected copy would otherwise fail to load entirely the moment the framework claimed the ids, which is an upgrade that breaks every existing adopter. Reading the process without owning it is `show-workflow`, which prints from `inspect.getsource` on the module that is actually imported -- so what a person reads is what runs, which a copy in their own file could never promise. `init --eject` was built first and dropped: it put the bodies back into the adopter's module and therefore reintroduced the import-merging machinery this row exists to retire, leaving duplicate imports whenever both verbs were ejected into one file (#260). Reading was the half people wanted; owning was the half that made every fix land twice. There is now no flag that writes a process into somebody's module, which is what makes one copy a fact rather than a default. Asserted as an absence of BODIES rather than a line count, which reformatting would satisfy. Filed as #239's neighbour; the assessment of 2026-09-05 named this the decision most likely to be regretted. |
| `GATE-DOCS-1` | none | held | Every Python snippet the docs show type-checks under the `mypy --strict` an adopter runs, given only what the page says it assumes. `test_docs.py` builds one module per page -- its blocks in order, as a reader pasting them into one file would have them -- under a prelude that binds exactly two names, `lockstep` and `ctx`, and runs the checker over all of them; a positive control feeds it the bare `Outcome` #252 filed and asserts it is refused. The prelude deliberately imports none of the framework's public names, because a prelude that supplied `Verb` would let a snippet omit the import a reader needs, which is the README front-door defect the execution gate beside this one exists for. Forty errors across three pages went with it: missing imports, untyped parameters, a bare `dict`, illustrative bodies strict mode refuses, and names the prose implied but no snippet defined. `GATE-PLUGIN-2` made an adopter's module checkable and everything `init` writes pass; this is the third place the same shape had to hold, and the docs were in neither of the first two sweeps. Since #311 the page that teaches the first thing an adopter tries is also rendered, not only type-checked: every block of `extending.md` that defines a prompt is executed in a scratch repository holding the body it names and the prompt composed, the pass `cookbook.md` already had, because the page showed `Body.from_file("prompts/our-review.md")` for months and the checker passed a shape that resolved inside the framework's own package. The strategy example is executed too and handed to `Lockstep.use`. |

## Policy

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-POLICY-2` | none | held | Every field `Policy` carries reaches something that enforces it, and `test_a_policy_field_that_enforces_nothing_is_not_offered` pins the set so a new one cannot arrive inert. It got there by subtraction. `network`, `permissions`, `max_ai_credits`, `per_run_ai_credits` and `daily_ai_credits` were merged by `resolve()`, printed by `ls`, reported in the receipt and read by nothing else — a security field that reads as in force while enforcing nothing, in the artefact a security reviewer trusts. Each already had a control covering the same ground (`EgressPolicy` and the egress probe; the `Sandbox` and `deny_tools`; `Budget` and `CostBudget`), so wiring them would have meant two writers of one rule and a second, weaker way to say what the first already enforces. #263 deleted them and `docs/extending.md` carries the migration line by line. That is O6's last blocker, and the mission's *policy of their choice* reads better for it: an adopter's remaining choices are the ones that bind. |
| `GATE-MODEL-1` | none | held | A verb that needs a model capability is refused a model whose registration says it does not have it, before the first turn and by name. Two capabilities are read, at the one place that knows the model: `AiInvoker.run(schema=...)` refuses `model.no_structured_output` when the registration declares `ModelCaps.structured_output=False` and the caller needs its answer in a schema, and `model.no_tool_use` when the registration declares `tool_use=False` and the session hands it tools. Every shipped AI verb passes its schema, the three implementing strategies through `run_phase`, so the refusal is on every live path and arrives as `BLOCKED` -- the adopter's choice declined, not broken -- with the message naming the model, the flag and the line to change. Nothing is sent and nothing is charged; the provider is not called. The field was declared for five providers and read by nothing for the whole pivot, and wiring it as declared would have refused this repository's own `triage` route: `local` said `False` from when the flag meant a native JSON mode, and the framework has never used one -- it puts the schema in the prompt and repairs once -- so the flag now means "answers a schema when asked", both flags default to capable, and `local` declares what the documented `$0` path relies on. An invoker built by hand with no declaration is not checked, and the attribute says why: unlike residency, silence here is not a control that must fail closed. `doctor` warns (`DOC152`) about a route to a registration that would be refused. `context_window` and `vision` stay declared and unread, and `ModelCaps` says so. Closed by #274. |
| `GATE-SHAPE-1` | none | held | The shape of a structured answer is a contract the framework holds, not a sampling outcome. Two clauses. **A reply that fails to parse or validate is re-prompted once, with the parser's own error quoted back, and never a third time:** `ai.structured.settle` takes the first invocation, and on a parse failure or a schema mismatch sends one more turn carrying the reply and the exact words the parser refused it with, inside the same adapter step so the second bill lands under the same budget middleware and the same recording as the first (O4). A truncated reply is not re-prompted -- an output cap is a number in the policy, and a re-prompt would pay for the same cut-off again -- and a second failure is reported under the verb's own `unparseable` or `schema_mismatch` reason naming both attempts, with the cost of both calls. Every single-answer verb goes through it: review, triage, rfe, backport and improve; the write verbs read their cover note leniently because the change already came through the tool boundary. Before this, three reviews on this repository's required check errored on `Expecting ',' delimiter` a few hundred characters in -- a malformed object, which bracket-closing cannot touch -- and each turned the check red on a change nothing was wrong with (#254). **The shape a skill shows the model is the shape the parser reads:** `review-format.md` told the model to write `{path, line, comment}` to an output path while `_to_report` read `{path, line, summary, detail, severity}` inline, so every review composed two contradictory contracts (#271). The skill now shows the parser's shape, and a test parses the skill's own example under the schema and reads non-empty fields from it. |
| `GATE-TEAM-1` | none | held | What one engineer's run produced is legible to the next, and a team can see whether two engineers running the same process get the same results. `history --explain` names who approved a run and who the host said asked for it, what it cost and what it decided. `report --by actor` splits the ledger by asker -- outcome mix, turns and spend per succeeded run, findings per run and what each keeps finding -- and prints the spread: per metric, the asker at the top against the one at the bottom, each with the runs it was measured over, so one run reads as one run and not as a verdict. Three disciplines, each asserted. Askers are pseudonyms numbered by first appearance in the window unless `--names` is passed, and the login is kept nowhere else on the row, so a page rendered from the default report cannot name a person by accident; the same pseudonyms key `--by actor --by-kind` and `--format json`. A run carrying neither an approver nor a host actor renders under `—` with its count, is not numbered, and is in no spread -- forty-five of this repository's fifty-six records are such runs, and a bucket called unknown would be its largest asker. Two spellings of one person are two askers, because merging them would be an aliasing decision the report has no evidence for. **What this repository's own ledger shows:** one human under two spellings, so the spread it prints is between spellings, not people; the mechanism is exercised by tests over a two-asker ledger, and the gate that needs a second engineer to have run here is not this one. `--by actor` is the one `--by` value that shapes the full report, re-decided in `report_cmd` rather than slipped past the note that says `--by` does not. Filed as #164. |
| `GATE-TEAM-2` | none | held | A local run carries who ran it, when the repository chose to say, so the consistency question can be asked of the runs a team actually makes. `ci_actor` exists only under CI and `approval.by` only when a grant was needed, so every local run -- forty-five of this repository's fifty-six records -- carried no identity at all and sat under `—` in every report, outside the spread. #164 named the remedy and refused to make it a default: recording the git author is a separate, opt-in decision, because a report that named people who never chose to be named is the leaderboard the pseudonyms exist to refuse. So it is one line, `lockstep.identity = GitAuthor()`, that a repository writes and nothing detects; a module that does not write it records exactly what it recorded before. What the line records is the configured git author as git would write it, `Name <email>`, both keys required because git itself requires both before it will author a commit and an author git invented from a hostname is a fact about a machine rather than a claim a person made. It is written as `identity`, a third field under a third name beside `ci_actor` (what the host said) and `approval.by` (what a grant claimed), so `history --explain` prints which kind of claim it is and `actor_of` reads it after the other two, because a claim with no host to corroborate it is the least specific of the three. The pseudonym rule applies unchanged: the report numbers the author by first appearance and names nobody unless `--names`, and two spellings of one person stay two askers -- which is what this repository's own ledger now shows, since its runs at a terminal carry the git ident and its runs under CI the host login, so the spread it prints is still between spellings of one engineer. The mechanism is asserted over a two-person ledger: a local run with the line set carries the identity end to end, a run without it carries none, and `report --by actor` counts the one and dashes the other. Closed by #289. |

## Packs

Post-1.0, from `design/extension-packs.md`. Extension packs distribute a strategy, a prompt or a
verb between repositories, and the gates below hold the property that makes that safe: installing
one **offers** it, and a line somebody wrote is what puts it in force.

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-PACK-1` | none | held | An `in_lockstep.extensions` entry point present in the environment produces **no** container binding after `Lockstep.detect()`, which does not query the group at all, asserted directly rather than inferred from an empty container. The contrast is `GATE-PLUGIN-1`: a standards package applies itself because it may only tighten, while a pack hands a model write and execute tools and pays for a model call, so its arrival is a diff. Listing a pack calls no code it ships: `installed()` never invokes `entry.load()`, and `pack.toml`, the corpus, the cassettes and the AST behind `imports` are read through distribution metadata rather than `importlib.resources`. |
| `GATE-BODY-1` | none | held | A house prompt body lives in `prompts/` at the repository root, that location is grantable, and a body that cannot resolve is caught by `doctor` and refused in one line, never a traceback. Two doc pages disagreed on where a body lives, and the one the framework's own hint gave, `.lockstep/prompts/`, is tier-1 deny-always: a lens there could be reviewed with and never proposed to by the learning loop, and neither page said so (#311). `Body.from_path` is a file in the repository; `Body.from_file` is a resource in a package and refuses without one, naming `from_path`, because a lifecycle module's "package" is the framework's own and a body the repository wrote was looked for among the ones we ship. `doctor` walks every bound `Inspectable` adapter's compositions and raises `DOC140` naming the lens, the path and the adapter; `show-prompt` and `review` turn `BodyNotFound` into a `ClickException`; `improve --explain` says a tier-1 body can never be granted and where one that can lives. Asserted over a bound lens with a missing body through `doctor` and `show-prompt`, the old `from_file` spelling, and the guard with a `prompts/` grant against both locations. |
| `GATE-PACK-2` | none | held | A layer stack that omits the shipped baseline is reported wherever it can be seen: `guardrails_intact` is `false` in the repository receipt for every prompt a bound adapter composes, and `doctor` raises `DOC171` naming each. A warning and never a refusal. Replacing the stack is legal, and the property is visibility, the same posture the standards layer takes about removal. Scoped to **bound** prompts on purpose: a pack's body has no projection until something composes it, so a pack nobody bound sends nothing. |
| `GATE-PACK-3` | none | held | A re-derived receipt whose capabilities exceed the accepted record refuses `add` without `--accept`, and the refusal records nothing: the file at `.lockstep/packs/<name>.json` still holds the narrower set afterwards, so a refusal cannot leave a repository having accepted what it declined. Drift that grants nothing new (a version, a prompt, a corpus case) is recorded without a flag and reported, because refusing over those would teach people to pass `--accept` by reflex. |
| `GATE-PACK-4` | none | held | `add` writes no bytes to `.lockstep/lockstep.py` under any flag combination, asserted by content over the flags rather than by reading the happy path. There is deliberately no flag that turns this off: that file can rebind any adapter, remove any middleware and grant any tool, and it is loaded from a trusted ref precisely because every line in it was typed by a person. |
| `GATE-PACK-5` | none | held | A pack is measurable on a lens it INVENTED, not only on one it overrides. `prompts/<aspect>.md` paired with `corpus/review/<aspect>-reviewer/` composes a lens and its cases are driven to a verdict; the same file with no cases beside it composes nothing, because `guardrails()` reads house rules out of that directory too and every `.md` becoming a lens would file a pack's house rules as `review.house`. The invented lens states its own `aspect` rather than inheriting `ReviewPrompt`'s generic one, or every such lens would report its findings as `review.review`. This is what `pack try` is for: a pack kind that cannot produce a number is a pack kind nobody should install, and prompt packs were that kind. |
| `GATE-RECORD-1` | none | held | Every CI review records its inference, and the recording never leaves the runner. Three clauses, all asserted over this repository's workflows **and** over both trampolines `init` scaffolds, because the adopter's file is the one the framework is making a claim about. (1) A review that reaches a provider passes `--record`; a review that reaches none is exempt by name, since `--offline` and `--dry-run` cannot also record and `_one_provider` refuses the combination -- the release smoke check replays the shipped fixture and asking it to record would be asking it to overwrite what it is replaying. (2) The tape is written outside the checkout (`RUNNER_TEMP` on GitHub, `/tmp` on GitLab) and no artifact path is absolute or reaches there, so "the tape stays on the runner" is enforced by where the bytes land rather than promised by a `path:` line. A cassette holds the request verbatim -- the whole composed prompt and the whole diff -- and `Redact` masks the credential shapes it knows, never source. (3) Every artifact carrying run evidence declares a retention, in each host's own word for it; the vendor default is a policy nobody chose, and this is half of N9's answer. (4) An upload naming a dotted path declares `include-hidden-files`. That clause is here because the first run of clause 1 produced a 1115-byte artifact holding the history bundle and no cases: `upload-artifact@v4` drops hidden files by default, `.lockstep/` is hidden, and the step goes green either way. The `path:` line had named `.lockstep/` for months and had never carried it. It is the failure this whole file is about -- a control that reads as though it were in force -- and nothing static caught it. Downloading the artifact did. What survives is the cases harvested from the tape, not the tape. Until this, every review this repository paid for was discarded, and so was every review in what an adopter was given. |
| `GATE-RECORD-2` | none | held | A workflow run keeps every model call the framework builds an invoker for, and the tape lives outside the repository. This is the flag `implement` and `fix` never had, and the reason was a seam rather than a policy: CI drives the write verbs through `in-lockstep run`, an adapter reached that way builds its OWN invoker via `routed_invoker`, and the CLI therefore had no provider object to wrap the way `review`'s closure does. `RunContext` carries the tape and `ai.bootstrap` wraps where it already attaches the transcript writer, so every adapter that lets the framework build its invoker records without opting in. **The boundary, stated because it is real:** an adapter constructed with its own `invoker_factory=` builds the provider itself and nothing can reach inside a lambda, so `--record` records nothing there — and rather than let `kept 0` mean both "no model was called" and "the recorder was bypassed", a run that spent tokens and kept nothing says which it was. Measured after the fact from the run's own spend, not predicted from the bindings, which would be wrong in both directions. The tape goes to `RUNNER_TEMP` or `$TMPDIR` on a laptop exactly as in CI, and a `--cassette` inside the repository is refused by name: a write verb's tape holds every file the session opened, and `read_file` refuses that directory (`GATE-GUARD-4`) precisely because a tape in the tree is the next run's prompt. Two locks, one door. A replay cannot also record — a provider declaring `transmits = False` refuses, which is `_one_provider`'s argument one layer down, where a module rather than a flag asks. |
| `GATE-RECORD-3` | none | held | What leaves a write verb's runner is addresses, not contents. A session is ONE case rather than one per turn — each turn's request is the one before it with a turn appended, so nine cases are nine copies of one question and the ninth contains the other eight. Sessions are segmented from `order` by the prefix-extension relation, never from `sorted(calls)`, which is hash order and says nothing about what followed what; two invocations in one tape (TDD's red phase and its green) have different system prompts, so no chain forms and they stay two cases, which is right because they are two questions. The case kept is the session's LAST measurable call, because a write verb's earlier turns answer with a tool call and empty prose and `_expect` can derive nothing from one — a case built on turn 1 would be a case that cannot fail. Its request therefore carries the whole session, which is exactly why every tool result and every over-long tool-call input is replaced by a marker: measured, a four-turn session's tape is 178,686 bytes of `provider_calls` and the case is 6,640, with the file bodies and the command output gone and the paths, the tool names and the question kept. Elided by SHAPE rather than by a list of tool names, so an adopter's own tool is treated the same (O8). **The elision is falsifiable or it is a fabrication:** per category a count, a byte total and a sha256 over exactly what was dropped in order, so somebody holding the tape reproduces the digest and sees that the case left out what it says it did — asserted by reproducing it, not by reading it. `recoverable` is honestly `false`: command output is not byte-stable and a file body is recoverable only against the commit it was read at, which a case does not record. A category nobody dropped is absent rather than written as zeros, so a `review` case carries no `omitted` block at all. |
| `GATE-RECORD-4` | none | held | A run that calls a model keeps it without being asked. Recording was `--record`, off, so `in-lockstep review` at a terminal spent money and kept nothing -- while every trampoline this repository ships and every one `init` scaffolds passed the flag. That is why it survived: the framework's own runs all satisfied O4 and the gap was exactly the path a person meets first, which is the shape that stays invisible longest. Tri-state rather than a flipped boolean, and that is the whole subtlety: `--offline` and `--dry-run` each say answers come from somewhere that is not a provider, so a default of plain `True` would contradict them on every offline run and refuse it. `None` means nobody chose, so only an explicit `--record` can still collide -- `GATE-RECORD-4` does not weaken the refusal `test_one_provider` holds, and that refusal is asserted through the new resolver as well as around it. Scoped to the six verbs that call a provider as their job; `pack try` keeps an opt-in flag because its default is a $0 replay and its own docstring calls recording the direction that spends, so a default there would make a measurement tool start spending. The `kept` line is silent when a run reached no provider and spent nothing, because `kept 0` for `run selfcheck` reports on something that did not happen -- but NOT when tokens were spent, which is the recorder being bypassed and still says so. Filed as #233. |
| `GATE-RECORD-5` | none | held | A model call made through an adapter that builds its own invoker is recorded. Two holes with one symptom, and each was invisible because the other hid it. The seam in `ai.bootstrap` wraps the provider the framework builds, and it fires on `ctx.recording` — which the five direct verbs never set, so a module-bound adapter recorded nothing and no bypass warning fired either, because the detection is reached from the same reporting path (#264). And an adapter given its own `invoker_factory=` builds the provider inside a lambda no seam can reach (#243). Every path anybody tested recorded; the two an adopter actually takes did not. Closed at both ends: the direct verbs hand the tape to the run, and `resolve_invoker` — now the single seam all six adapters go through, where five spelled it out inline — wraps **what the factory returned**. That is the third answer to the choice the issue framed: putting the tape in the factory's signature moves the obligation onto whoever takes the escape hatch and an adapter that ignores it is back where we started silently, while refusing a custom factory during a recording run removes an extension point O8 exists to protect. Nothing can reach inside the lambda; the framework holds the object the lambda returned. `recorded` is idempotent and that guard is load-bearing rather than defensive — on the commonest run of all, the CLI's own factory wraps *and* the tape is on the context, so without it every inference would be written twice and the ledger would report a run that made double the calls it made. The outer boundary is tested rather than promised: a bespoke adapter that takes no factory and builds the invoker inside `invoke` touches no seam, cannot be wrapped, and is *named* — the run compares the tape against its spend and says a model was called the recorder never saw, rather than reporting a reassuring zero. Closed by #243 and #264. |
| `GATE-FIXTURE-1` | none | held | Hashing the request that ships in `cassettes/request.json` reproduces the shipped cassette's only key, so the demo recording can never be shipped unreplayable. It exists because the fixture used to be a response beside a hash: replayable but unreadable, and undiffable against what the code composes now. The first prompt edit after it was recorded turned the first command a new adopter runs into a key miss they had no key to fix. `FixtureProvider` now falls back to the recorded request and **says** the prompt moved; the fallback is confined to the shipped demo, and `ReplayProvider` stays strict for a user's own recording, where serving the old answer to a new question would fabricate a result. `harvest` reads the same sidecar (#269), so the recording an adopter can replay is also the one they can harvest without spending. Until then the reader counted it as recorded before requests were stored and told them to re-record it -- which is the one thing the fixture exists so they never have to do. |
| `GATE-REVIEW-1` | none | held | The conversation gathered from a pull request comes only from branches this framework wrote. `changes_for` matches `branch_for`'s layout (never the title, the body or a linked-issue reference), so a change request a stranger opened saying "fixes #218" is not gathered as though a reviewer of *our* change had written it; without that, opening a pull request nobody merges would be a writable channel into an agent holding write tools. Everything gathered arrives through `Ticket.as_context`, the single tagging point `Implement.ticket` calls "the only route in", as `UNTRUSTED_EXTERNAL`, the same provenance as the ticket body, because a reviewer's note is a person writing at a model. The framework's own sticky-comment marker is stripped on the way. A host that cannot read a conversation loses the context and not the run, and says which. |
| `GATE-REVIEW-2` | none | held | A `/fix` or `/implement` asked for on a pull request runs against the ticket that pull request was opened for, or refuses by name. The number a comment carries is resolved in Python. The trampoline passes it through untouched, so the decision is not an `if:` expression, where being wrong reads exactly like "nobody asked". Resolution reads the record `change_body` wrote and falls back to the branch only when the branch's shape is unambiguous, so it can decline to resolve a real ticket but never resolve the wrong one. A change request that records no ticket raises `NoTicketForChange` naming it, rather than proceeding with a pull request's own number and failing two steps later with a true sentence about the wrong thing. A host that numbers issues and change requests separately is not guessed at: `shared_numbering` is a fact about the host, and where it is false the key is left alone. |
| `GATE-REVIEW-3` | none | held | The lens a review is asked for — by a `/review` comment or by `--aspect` at a terminal — is resolved in Python, against the lenses the BOUND adapter has, before any credential is read or any run id exists. A name that is no lens is refused with the set that does exist, reads no key, and appends no ledger record — which matters because the adapter's own refusal arrives after `_run_id`, and `blocked` sits inside `failure_rate`'s denominator, so a stream of typos from anyone who can comment would deflate the repository's own failure rate. The flag took a different road until #275: it reached the adapter, and a typo at the terminal wrote a `blocked` record, the framework's word for a control stopping a run, spent on nothing stopping anything. One function, `chatops.resolve_aspect`, serves both spellings now, so they cannot drift apart again. The two part only on a bound adapter that is not `Inspectable` and so states no set at all: the flag's name passes through to the adapter's own refusal, because a set nobody stated is not one to refuse against and the shipped four would be a guess about a stranger's adapter; a comment is refused by name, because a comment is anyone's and the closed set is the whole promise. The closed set kept an aspect out of `report.marker`, which builds an HTML comment by interpolation, and since #275 the marker escapes its argument as well — a `-->` inside the kind closed the comment early and orphaned the anchor the next run finds its own comment by — because a defence that is only an ordering is one reordering away from gone. The escape is injective and leaves every shipped kind byte-identical, and its pattern is defined beside the writer and imported by `comment`, whose own copy excluded `-` and refused the body a hyphenated lens had written as unanchored. Resolution cannot be delegated to the trampoline in any case: GitHub's expression language has twelve functions and none of them splits a string. |
| `GATE-REVIEW-4` | none | held | A finding names coordinates the change actually touched, or they are refused rather than reported. `_in_the_change` checks each finding's `path` against `Diff.paths` and its `line` against `Diff.hunks`, over the diff **as sent to the model** — not a fresh `git diff`, because a model cannot name a file it was not shown, so the context is the tighter test and a review that reached the model must not then fail on a ref resolving. The two halves are refused differently, and the difference is the argument: a path the change does not touch drops the **finding**, because there is nothing there to say anything about; a line outside every hunk drops only the **line**, because a model that noticed something real about the file and pointed one line off is still worth reading. Per finding, so one bad path does not discard three real findings beside it, and both are **counted** — `review.path_not_in_diff` and `review.line_not_in_hunk` name what was refused, because a lens that quietly drops half its output reads as a lens that found half as much. Both sides of the diff count as touched: a deleted file appears only under `--- a/`, and deleting one is a fine thing to have an opinion about. Coordinates are never rewritten to make them match — `./x` is normalised, `a/x` is not, since `a/` is a legal directory name and rewriting until something matches is the guessing O1 refuses next door. Three places fail open rather than refuse, each tested: a diff with no parsable path (a pure mode change), a file with no new side (a deletion), and a finding claiming no line at all — in each, *cannot be checked* is a different fact from *is wrong*, and enforcing the rule on input that cannot answer it would drop real output. Closed by #234. |
| `GATE-REVIEW-5` | none | held | The required review check exercises every lens the BOUND adapter declares. `review` is a required status check on `main`, so every pull request here was already gated on a model reading the diff -- through one lens of four. `intent`, `performance` and `tests` shipped, composed, and sat in the characterization corpus without ever having run on a real pull request in the repository whose whole argument is that it runs on itself, which is O10's definition of asking adopters to trust us on our word. It bounded O5 as well: this check records and harvests, so a single-lens check made a single-lens corpus and #163's loop would have had evidence about a quarter of what ships. Compared against `_review_lenses` over this repository's loaded module rather than against a list of four written in the test, because a gate that hardcodes the shipped set goes stale the moment somebody adds a fifth -- which is the failure it exists to prevent one layer down, and is asserted by adding one. Both directions: a bound lens the loop never runs, and a name in the loop no adapter declares, the second because `GATE-REVIEW-3`'s refusal is loud but arrives after the job has installed the provider extra and minted a credential. **Since Phase 5 (PR-13)** the check is `review/all-lenses`, a shipped workflow whose branches are declared from the bound adapter's `compositions()` and fanned out under one budget, one tape and one kill switch (`GATE-COST-6`, `GATE-ASYNC-3b`), so `lockstep.yml` can name no lens at all and the list that could go stale is gone. The file flipped one merge after the module registered the workflow, not in the same pull request: configuration loads from the trusted ref while a pull request's workflow file comes from the merge ref, so the pull request that registered it ran a check naming a workflow the base branch's module did not know (run 34171482674). The gate is asserted over the workflow with a stub adapter declaring five lenses, one of them nothing the framework ships, in both directions, and over the file in whichever spelling it carries: the legacy step must list every bound lens, the fan-out step must list none. Four lenses are defensible on a REQUIRED check because findings are non-blocking warnings -- a lens that finds something still exits 0, so nothing here turns a note about performance into a merge block. Scoped to this repository and not to what `init` scaffolds: exercising every lens is a decision about money, and quadrupling a stranger's first bill is not ours to make. The scaffold names the other three in a comment instead. Filed as #241. |
| `GATE-REVIEW-6` | none | held | Every lens the required check runs says what it found **on the pull request**, as its own sticky comment. Four lenses had read every pull request here since #241 and said nothing on any of them: their verdicts and findings went to the job log, so the reader saw a green check and none of what a model had been paid to say -- and a later `/fix` on the ticket, which gathers what was said on the change request, gathered none of it either. `review` with several `--aspect`s and a `--comment-out` directory writes `<lens>.md` per lens, each anchored by its own `review:<lens>` marker, and refuses a path that says it is one file rather than overwriting it once per lens; the artifact carries the directory; and `comment --body-file <dir>` posts every body in it under the marker inside it, from the `publish` job -- write token, no provider credential, the split `GATE-LEDGER-10` made -- checking every body for its marker before posting any. One comment per lens rather than one wall, because a reader telling `security` from `tests` should not have to scroll a table, and because a re-review then edits four comments in place. Asserted at the command (bodies, markers, the refusal, the all-or-nothing post) and over this repository's workflow and both scaffolds. `/review <lens>` on request already posted this way through `review.yml`; the required check now says as much as the lens somebody asked for. Closed by #345. |
| `GATE-DOGFOOD-1` | none | held | The loop has closed on this repository, and the published ledger and the host say so. O10 said "it runs on itself" while the framework had never opened a pull request here (#312): eight implement and fix runs cost $275 and the two whose work half succeeded died at `propose`. Nine `/fix` runs on #319 later (#333 to #342 each fixed what the previous run surfaced: a redaction that wrote `***` into a test, an image without git, a container without the venv, a capability-dropped user who could not write `.ruff_cache`, a uid no `getpwuid` knew, a session that scripted string replacements for want of an edit tool, a session that fixed inside its reproduce step), run 34158960476 staged a reproducer and a fix, proposed #343, and #343 merged on 2026-09-07 -- the first framework-authored change in this repository's history. `improve.yml` ran twice (34069517686 dispatched, 34121918174 scheduled), each blocked by name, `improve.no_trend` then `improve.nothing_to_improve`, which is the loop refusing to propose on no evidence. Asserted by reading `origin/lockstep-history` for a succeeded fix or implement record and an improve record, and by asking the host for a merged pull request on a run branch; each half skips with a named reason where its source is out of reach, and `ci.yml` checks out every branch and hands `gh` a read-only token so on CI it runs. **The chat-ops clause closed on 2026-09-07:** `/review security` on #346 (run 34163733667) was the first comment that ever matched `review.yml`'s gate, and it failed one step in -- this repository's file handed its reviewing step no `GH_TOKEN` while the scaffold did, and `change_refs` read "could not ask" as "not a pull request" (#347) -- and the second, on #348 (run 34167168362), ran gate, review and post through to the sticky comment. Asserted by asking the host for a succeeded `review.yml` run. What that run showed next: its record was uploaded as `review-<n>`, a name the sweep never lists, so it would have expired unabsorbed; every workflow that bundles a record now uploads it under the one name the sweep lists, and a test holds every file and scaffold to it. Two smaller facts the same issue found are closed beside it: a run id only a fixture mints (`triage-<n>`, `<name>-local`) is refused by every path that publishes -- push, reconcile, absorb -- naming it, while the four that already leaked stay pullable until the acknowledged rewrite that removes them, which is a person's act; and `doctor` warns (`DOC127`, `DOC128`) when `enforce_admins` is off or a bypass actor sits on the ruleset that requires the review check, because a `required` check one engineer can step around is enforced by choice, and here it is. Filed as #312. |
| `GATE-LENS-1` | none | held | A review lens is a declared object keyed on the name it already has. `Lens` carries the prompt, an emphasis, a layer stack and its ceilings, and `review/<aspect>` is a route key that wins over `review`; `AiReview` composes each lens from its own declaration, so `show-prompt` renders the emphasis and the stack a run would send, `ls` prints one guardrail chain per distinct stack rather than the first lens's as everybody's, and a route to a lens nothing binds is flagged beside it the way a route to a verb nothing serves already was. The key is the whole design and not a convenience: `review.security` is the finding id, the sticky-comment marker, the `Improvable` label and the census key at once, and an enhancement that renamed the lens would fork all four -- which is why #204's design panel refused a verb per lens (dispatch keys on the request type, so four verbs are four request types, four adapters and four binds, and the same again for every extender). A lens differs in what it **says**; a verb differs in what it **may do**, and `docs/extending.md` draws the line. Ceilings move one way each, and the reason is stated at the type: `max_turns` may only tighten, because the adapter's cap is already `min(its own, the policy floor)` and the floor is not recoverable from the result; `max_tokens` replaces, because `Policy` deliberately carries no floor for it. `Pack.emphasis` is the no-subclass form for a pack, and the bare prompt class stays a legal entry, so nothing written before this row changes meaning. Closed by #204. |
| `GATE-EVAL-3` | none | held | A recording can always find itself — given the request as it was SENT, or as it was STORED. **The original wording said one thing and the fixture proved a weaker one.** It claimed a stored request hashes back to the key it is filed under; it does not, and cannot: the filing key hashes the raw request because a live lookup holds a raw request, while the entry holds `redact.text(...)` of it because a file on disk must not carry a credential. Both are right and the two hashes are simply different numbers. The assertion survived because every fixture recorded through a `Redact` with nothing registered and no structural pattern in the text, so the redactor was a no-op and the property could not fail however wrong it was — the same shape as `GATE-RECORD-1` clause (4), one file over. `Cassette.as_stored` is the index that reconciles them, derived at read time so no tape migrates, and `replay_provider` consults it only after a miss. Asserted in both directions, over a fixture that masks something: neutralising the fallback turns the stored lookup red while the live lookup stays green, and the test refuses a fixture with nothing to redact rather than passing over it. Everything harvesting does rests on this: a stored request that could not be found produced cases that build, replay, miss, and report as unplayable for a reason nobody could act on — a code fault wearing the costume of a data problem. Harvest refuses rather than guesses on a recording made before requests were kept, invents no rubric, and never writes: a case holds a whole request and a whole answer, so the write is a redaction sink and belongs to a layer that may reach `privileged`. |
| `GATE-EVAL-4` | none | held | A harvested case settles with no cassette on disk, and a case whose request and answer were never a pair is refused rather than graded. A case that only pointed at a cassette was worth whatever that cassette still was, which for the recordings worth harvesting is nothing: a CI recording is written to the runner's temporary directory and destroyed with the runner (`GATE-RECORD-1`), so the case arrived in an artifact beside a path on no machine and `eval run` called it unplayable for a reason nobody could act on. The answer now travels in the case, in the same field set `replay_provider` reconstructs an `LLMOutput` from, so settling it offline and settling it from the tape give the same verdict. Being self-contained means being editable, so the pair is checked rather than assumed: `harvested.key` is the request's hash as recorded, and a request that no longer hashes to it is carrying somebody else's answer -- settled, that would be a question graded against the reply to a different one, which is the fabrication this corpus exists to refuse. That is `GATE-EVAL-3`'s property one row up -- a recording can find itself -- carried forward to the artifact that outlives the recording, and it matters more here: a cassette is a blob nobody opens, while a case is a file in a repository that people edit and cherry-pick. The cassette fallback stays for every case harvested before the field existed. |
| `GATE-EVIDENCE-1` | none | held | A promoted case is settled by `make check`, and the promoted set is capped. **Both clauses hold, and the first now has a subject.** The cap: `evidence/cases/` admits 24 per family and `test_evidence.py` turns the 25th red, with a positive control that drives the counter over a tree built to break it -- load-bearing in a way a control usually is not, because for as long as the corpus was empty an assertion over zero directories passed however wrong it was. The settlement: `make check` and `ci` run `eval run --corpus evidence/cases`, asserted from the Makefile the way `GATE-TEST-3` asserts `make cov` runs, and one case has been through it -- `evidence/cases/review/lockstep-48-security.json`, the shipped demo's recording of a real model reviewing a real merged change, promoted by #268 once #269 taught `harvest` to read the sidecar its request ships in. It settles on every commit: `1 replayed, 0 skipped, decided 1`. **What `held` means here, and what it does not.** One case, from the one recording that already ships in the package, so the promotion published nothing that was not already public -- which is why it could be the first. The path this corpus was written for -- a CI run's artifact, downloaded and read and copied by a person -- has still never carried a promotion, and this row does not claim it has. What it claims is that a case in this directory is measured on the commit that adds it, and that a case which would skip cannot be in it. **What green does not mean:** `eval run` exits 0 on a SKIP, which is right in general -- the twenty-seven hand-written cases carry no request and skipping them is honest -- and wrong for a corpus whose premise is that what is in it settles, so a promoted case that skips would be a published prompt buying nothing, forever green. Both ways one can skip are refused in `test_evidence.py` by name: no recorded request, and a request that no longer hashes to the key it was recorded under. **Promotion is not automated, and that is enforced rather than asserted:** this repository's `lockstep.py` puts `evidence/` in tier 1 of its `PathPolicy`, so `ChangeGuard` refuses the path at the write-tool boundary and again at `apply`. That line exists because the first draft of this row claimed no job could write there while `check_path` returned `None` for it -- a `fix` run given a ticket mentioning the eval corpus could have staged a case and the `propose` job, holding `contents: write`, would have committed it. A control that reads as though it were in force is what this file's opening argument is about, and writing one into the gate ledger itself is how close that failure sits. It is this repository's line, not the framework's: `evidence/` is a convention, and an adopter wanting automated promotion simply does not write it. |
| `GATE-EVAL-2` | none | held | No shipped case states an expectation no answer could satisfy. `Case.parse` already refuses an expectation nothing checks, on the grounds that a case that always passes is not coverage; this is that argument from the other side, and it went unstated while twelve of the twenty-seven shipped cases broke it. `count` compared a length to its expectation with `==`, so the twelve that say `{"min": 1}` -- meaning "found something" -- were pinned at failed for every answer any model could give. Five of them are in `review`, the only family `trial` drives, so a pack that answered every review case perfectly scored 4 of 9: a measurement taken against a comparison that was never made, which is this project's own failure mode run backwards. The comparators are now a closed set validated at parse, so an unrecognised one is a refusal rather than a case nobody can pass, and the corpus is checked by search rather than by reading: every count expectation must admit some length. |

## Improvement

The loop that reads the ledger and proposes a prompt change (#163). Four of these rows are `unmet`
on purpose and stay that way until the mechanism each names exists: the first pull request builds
everything that **refuses**, and a gate marked `held` over a mechanism nobody wrote is the exact
decay the status column at the top of this file was added to stop.

| Gate | Phase | Status | Assertion |
|---|---|---|---|
| `GATE-IMPROVE-2` | none | held | A proposal drafts a change to exactly one declared `Improvable` body and to nothing else -- not to a second body, not to `.lockstep/lockstep.py`, and not to a path a model named. Structural at both ends. The drafter is asked for the whole revised body as text and never for a path: the `ChangeSet` is built by `improve/measure` from the declaration's own `body`, with the two-key header re-attached byte for byte, so there is no path for a model to name. And `improve/propose` checks it again where the write token is, because the artifact came from another job: more than one change, a path the lifecycle does not declare, or a deletion is refused as `improve.not_one_body` before anything is opened. |
| `GATE-IMPROVE-1` | none | held | `improve --explain` reaches no model, writes no ledger record and opens nothing: the record count in the store is identical before and after. `improve` without the flag runs `improve/measure`, and it spends nothing until every precondition holds -- a finding that clears both thresholds on billed runs, a declared body it is attributed to, a grant to the proposing workflow, and a promoted case the body as it stands fails; each refusal is BLOCKED before the first model call, exit 3. A refusal is a run and leaves a record, which is safe since #256: the census excludes refused records by status, so the record a refusal writes cannot become the trend the next tick proposes from -- asserted by reading the census back after one. This row used to say the bare command exits 3 because nothing drafts a change; #163 built the mechanism and the row moved to the preconditions, which is what it was always about. |
| `GATE-IMPROVE-5` | none | held | Every count the trend census prints carries the population it was counted over — `runs` against the records walked, `blocking` against that finding's own runs, `weeks` against the runs anybody could date — and a run with no `ts` is absent rather than placed in week zero: a finding whose runs are all undated renders `—  (0 of N dated)`, and one partly dated renders its week count beside `(M of N dated)`. Four of this repository's own 27 records predate `ts`, and four finding ids are in exactly that state, so `0 weeks` would read as "measured, and it does not recur" for evidence nobody can place at all. Occurrences and runs are counted separately for the same reason: `review.security` is 26 occurrences over 13 runs, and a threshold reading 26 would say it came back twice as often as it did. The thresholds travel on the census rather than being passed to the renderer separately, so a header cannot name a number the flag beside it was not decided under; and a qualifying row is never truncated away behind `TOP_N`, because the census is ordered by runs and that is only half of what qualifying means. |
| `GATE-IMPROVE-6` | none | held | A trend is attributed to a body a `lockstep.improve` entry declares, matched on the finding id exactly, or to nothing. It is never inferred from the id's shape — `review.security` and `prompts/review/security.md` share a word and that is not evidence. The guard's verdict on the declared path is printed beside it, and where no tier matches the path the output says **permitted by omission**, because "nothing names this path" and "something granted this path" are different facts and only one of them is permission. |
| `GATE-IMPROVE-7` | none | held | The open-proposal ceiling refuses on an answer it did not get. A host with no `open_changes_by_workflow`, one that raises while listing, and one whose listing may have been truncated all exit 3 rather than reading as zero open. This inverts the `report --scm` idiom deliberately: a report degrades and says why, because a missing column costs a reader a column, where a ceiling nobody could read is not a ceiling. The actor decision is made first, so a login the gate refuses never causes a host call — the flag adds a network reach to a command that had none, and it must not be reachable by someone the gate was going to turn away. |
| `GATE-IMPROVE-3` | none | held | A drafted prompt change is written under a tier-2 grant scoped to the proposing workflow, so the prompts tree is writable **by grant** and never by omission. Two questions of the guard, asked by `improve/measure` before it spends: with no workflow the path must be refused (a tier names it), and with `improve/propose` it must be permitted (that workflow holds the grant). *Permitted by omission* -- no tier names it -- is itself a refusal, `improve.permitted_by_omission`, whose message says which lines in `lockstep.py` grant it. This repository's lifecycle names `src/in_lockstep/prompts/` in tier 2 and grants it to `improve/propose` alone; `test_own_ceilings.py` asserts every other workflow is refused there, and `GitLocal.apply` enforces the same guard at the write, so the grant is checked once before the money and once at the bytes. |
| `GATE-IMPROVE-4` | none | held | A proposal is measured against the harvested corpus before it is opened, both arms over the same denominator, and a proposal that does not improve the measurement is not opened. The denominator is every promoted case whose recorded prompt carries the body verbatim; a case recorded against an older body is `unattributable`, counted and named. The before arm is the recorded answer graded by the grader `eval run` uses; the after arm is the same recorded request with the body swapped and nothing else changed, re-asked of the model the case was recorded on, and graded the same way; a case the after arm could not answer leaves both arms, named. `improved` is a case the current body fails and the draft passes with nothing lost; one lost case is `regressed` whatever was gained; neither `regressed` nor `unchanged` is staged where `propose` reads, and `propose` refuses a staged scorecard that says anything but `improved`. What makes this measurable at all is stated in `improver.py`: a harvested case passes the answer it was harvested with by construction, so a case the current body fails is one a person tightened, and a corpus nobody tightened is refused before spending as `improve.nothing_to_improve` -- which is this repository's state today, one promoted case that passes. **What this does not measure:** whether the new answer is *better* in the sense a rubric would settle. A rubric is `outstanding` on both arms, the person on the pull request is the judge the loop declares, and no judge model is bound; the row does not claim one. The after arm is re-asked through the shipped factory, not only through a stubbed one: for as long as harvest stored the bare model id the provider was sent, the shipped path parsed no provider from it and raised a credential error after the drafter had been paid, and every test of the arm passed a `probe_factory` that never met the join (#310). The registry now stamps its registration name on the provider it builds, a recording keeps it beside the request, a harvested case carries `<registration>:<model>` and harvest never guesses one; a case that names no provider is refused as `improve.model_unqualified` before the drafter is invoked, naming the case and the fix; and a probe factory that cannot be built is `improve.probe_unbuildable` carrying the answers so far, not an exception after spend. `test_gate_improve_4_the_after_arm_is_re_asked_through_the_shipped_factory` runs `Measure` with no `probe_factory` against a registry holding a canned provider. |
| `GATE-IMPROVE-8` | none | held | The open-proposal ceiling is enforced where a proposal is opened, not only in a preflight command a pipeline may forget to call. `improve/propose` counts the host's open change requests on its own branch family with the posture `gate` established: a host that cannot list, or whose listing raised, is refused as `improve.ceiling_unread`, and a count at or over `max_open_proposals` -- snapshotted onto the run context like `max_attempts` -- is refused naming the open ones. `GitLocal` learned to count so the ceiling holds at a terminal too (O3): an open proposal there is a run branch that still exists. The trampoline still runs the preflight, so a scheduled run costs nothing when one is open; the preflight is an economy and this is the control. |
