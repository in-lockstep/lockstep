# GENERATED by lockstep 0.1.0 — do not edit.
# Edit the spec, add an overlay, or run `lockstep eject` on this file.
# sources: commands/discover.md@3f9c382f pipeline.yaml@4feed875
# profile: my-app
name: discover
on:
  workflow_dispatch:
    inputs:
      profile:
        description: App profile to target
        type: string
        default: ''
      force:
        description: Re-run steps even when a cached output exists
        type: boolean
        default: false
      force_steps:
        description: Comma-separated step ids to force
        type: string
        default: ''
  workflow_call:
    inputs:
      profile:
        type: string
        default: ''
      force:
        type: boolean
        default: false
      force_steps:
        type: string
        default: ''
    secrets:
      APP_PASSWORD:
        required: false
      JIRA_API_TOKEN:
        required: false
permissions:
  contents: read
concurrency:
  group: discover-my-app
  cancel-in-progress: false
env:
  OUTPUT_DIR: outputs
jobs:
  discover-api-surface:
    environment: my-app-staging
    runs-on: ubuntu-24.04
    container: ghcr.io/pipeline-fw/exec@sha256:71c2f5a9d3e84b6c0a1f7e2d5b8c9a4e3f6d0b1c2a5e8f7d4c3b6a9e0f1d2c3b
    env:
      MY_URL: ${{ vars.APP_URL }}
      PROFILE_URL: ${{ vars.APP_URL }}
      MY_API_URL: ${{ vars.API_URL }}
      PROFILE_API_URL: ${{ vars.API_URL }}
      MY_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      PROFILE_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      MY_PASSWORD: ${{ secrets.APP_PASSWORD }}
      PROFILE_PASSWORD: ${{ secrets.APP_PASSWORD }}
      MY_AUTH_METHOD: jwt
      PROFILE_AUTH_METHOD: jwt
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
    - uses: pipeline-fw/pipeline-actions/restore@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
    - name: Discover API surface
      run: uv run python3 scripts/discover-api.py --output=outputs/api-endpoints.json --api-url=${{ vars.API_URL }}
    - name: Discover UI structure
      run: uv run python3 scripts/discover-ui.py --output=outputs/ui-structure.json
    - name: 'post: Discover UI structure'
      run: echo "discovery complete"
    - id: save-workspace
      uses: pipeline-fw/pipeline-actions/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
      if: ${{ always() }}
