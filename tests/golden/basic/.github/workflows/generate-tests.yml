# GENERATED by lockstep 0.1.0 — do not edit.
# Edit the spec, add an overlay, or run `lockstep eject` on this file.
# sources: commands/generate-tests.md@440e3547 pipeline.yaml@4feed875
# profile: my-app
# overlays: overlays/github/generate-tests.yml@33f7278c
name: generate-tests
on:
  workflow_dispatch:
    inputs:
      jql:
        description: Override JQL to target specific features
        type: string
        default: ''
      skip_discovery:
        description: Skip the discovery phase
        type: boolean
        default: false
      force:
        description: Re-run steps even when a cached output exists
        type: boolean
        default: false
      force_steps:
        description: Comma-separated step ids to force
        type: string
        default: ''
  schedule:
  - cron: 0 2 * * 1-5
  workflow_call:
    inputs:
      jql:
        type: string
        default: ''
      skip_discovery:
        type: boolean
        default: false
      force:
        type: boolean
        default: false
      force_steps:
        type: string
        default: ''
    secrets:
      APP_PASSWORD:
        required: false
      JIRA_API_TOKEN:
        required: false
permissions:
  contents: read
concurrency:
  group: generate-tests-my-app
  cancel-in-progress: false
env:
  OUTPUT_DIR: outputs
jobs:
  discover-application-structure:
    if: ${{ inputs.skip_discovery != true }}
    uses: ./.github/workflows/discover.yml
    with:
      profile: my-app
      force: ${{ inputs.force }}
    secrets:
      APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
  fetch-issues:
    needs: discover-application-structure
    if: ${{ !failure() && !cancelled() }}
    permissions:
      contents: read
      actions: read
    environment: my-app-staging
    runs-on: ubuntu-24.04
    container: ghcr.io/pipeline-fw/exec@sha256:71c2f5a9d3e84b6c0a1f7e2d5b8c9a4e3f6d0b1c2a5e8f7d4c3b6a9e0f1d2c3b
    outputs:
      items_extract_stories_from_each_issue: ${{ steps.fanout-extract-stories-from-each-issue.outputs.items }}
    timeout-minutes: 30
    env:
      MY_URL: ${{ vars.APP_URL }}
      PROFILE_URL: ${{ vars.APP_URL }}
      MY_API_URL: ${{ vars.API_URL }}
      PROFILE_API_URL: ${{ vars.API_URL }}
      MY_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      PROFILE_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      MY_PASSWORD: ${{ secrets.APP_PASSWORD }}
      PROFILE_PASSWORD: ${{ secrets.APP_PASSWORD }}
      MY_AUTH_METHOD: jwt
      PROFILE_AUTH_METHOD: jwt
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
    - uses: pipeline-fw/pipeline-actions/restore@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
    - id: cache-fetch-issues
      uses: pipeline-fw/pipeline-actions/step-cache@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
      with:
        step: fetch-issues
        key-prefix: ls-v1-user-story-validation-generate-tests-fetch-issues-372b2a80
        key-inputs: |
          .pipeline/step-defs/generate-tests.fetch-issues.json
          scripts/fetch-issues.py
        outputs: |
          outputs/jira-issues.json
        force: ${{ inputs.force }}
        force-steps: ${{ inputs.force_steps }}
        key-extra: ${{ inputs.jql }}
    - name: Fetch issues from Jira
      id: fetch-issues
      if: ${{ steps.cache-fetch-issues.outputs.hit != 'true' }}
      run: uv run python3 scripts/fetch-issues.py --output=outputs/jira-issues.json --jql="${{ inputs.jql }}"
    - name: Publish fetch-issues outputs
      if: ${{ steps.cache-fetch-issues.outputs.hit != 'true' }}
      uses: pipeline-fw/pipeline-actions/step-cache/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
      with:
        step: fetch-issues
        outputs: |
          outputs/jira-issues.json
        key: ${{ steps.cache-fetch-issues.outputs.key }}
    - name: Fan out issues
      id: fanout-extract-stories-from-each-issue
      run: pipeline-exec fanout --input=outputs/jira-issues.json --key=key --max=256 --only-missing --output-dir=outputs/extracted-stories --no-shard
    - id: save-workspace
      if: ${{ always() }}
      uses: pipeline-fw/pipeline-actions/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
  extract-stories-from-each-issue:
    needs: fetch-issues
    if: ${{ !failure() && !cancelled() }}
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        item: ${{ fromJSON(needs.fetch-issues.outputs.items_extract_stories_from_each_issue) }}
    uses: ./.github/workflows/aw-story-extractor.lock.yml
    with:
      item: ${{ toJSON(matrix.item) }}
      output_path: outputs/extracted-stories/${{ matrix.item.key }}.json
    secrets:
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
  verify-extract-stories-from-each-issue:
    needs:
    - extract-stories-from-each-issue
    - fetch-issues
    if: ${{ !cancelled() }}
    runs-on: ubuntu-24.04
    container: ghcr.io/pipeline-fw/exec@sha256:71c2f5a9d3e84b6c0a1f7e2d5b8c9a4e3f6d0b1c2a5e8f7d4c3b6a9e0f1d2c3b
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
    - uses: pipeline-fw/pipeline-actions/restore@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
    - name: Verify Extract stories from each issue coverage
      id: verify-extract-stories-from-each-issue
      run: pipeline-exec fanout-verify --dir=outputs/extracted-stories --min-success-rate=0.8 --expected='${{ needs.fetch-issues.outputs.items_extract_stories_from_each_issue }}'
    - id: save-workspace
      if: ${{ always() }}
      uses: pipeline-fw/pipeline-actions/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
  build-test-manifest:
    needs: verify-extract-stories-from-each-issue
    if: ${{ !failure() && !cancelled() }}
    permissions:
      contents: read
      actions: read
    environment: my-app-staging
    runs-on: ubuntu-24.04
    container: ghcr.io/pipeline-fw/exec@sha256:71c2f5a9d3e84b6c0a1f7e2d5b8c9a4e3f6d0b1c2a5e8f7d4c3b6a9e0f1d2c3b
    env:
      MY_URL: ${{ vars.APP_URL }}
      PROFILE_URL: ${{ vars.APP_URL }}
      MY_API_URL: ${{ vars.API_URL }}
      PROFILE_API_URL: ${{ vars.API_URL }}
      MY_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      PROFILE_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      MY_PASSWORD: ${{ secrets.APP_PASSWORD }}
      PROFILE_PASSWORD: ${{ secrets.APP_PASSWORD }}
      MY_AUTH_METHOD: jwt
      PROFILE_AUTH_METHOD: jwt
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
    - uses: pipeline-fw/pipeline-actions/restore@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
    - id: cache-build-test-manifest
      uses: pipeline-fw/pipeline-actions/step-cache@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
      with:
        step: build-test-manifest
        key-prefix: ls-v1-user-story-validation-generate-tests-build-test-manifest-372b2a80
        key-inputs: |
          .pipeline/step-defs/generate-tests.build-test-manifest.json
          outputs/extracted-stories
          scripts/save-manifest.py
        outputs: |
          outputs/test-manifest.json
        force: ${{ inputs.force }}
        force-steps: ${{ inputs.force_steps }}
    - name: Build test manifest
      id: build-test-manifest
      if: ${{ steps.cache-build-test-manifest.outputs.hit != 'true' }}
      run: uv run python3 scripts/save-manifest.py --input=outputs/extracted-stories --output=outputs/test-manifest.json
    - name: 'on-failure: Build test manifest'
      if: ${{ failure() }}
      run: echo "manifest build failed"
    - name: Publish build-test-manifest outputs
      if: ${{ steps.cache-build-test-manifest.outputs.hit != 'true' }}
      uses: pipeline-fw/pipeline-actions/step-cache/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
      with:
        step: build-test-manifest
        outputs: |
          outputs/test-manifest.json
        key: ${{ steps.cache-build-test-manifest.outputs.key }}
    - id: save-workspace
      if: ${{ always() }}
      uses: pipeline-fw/pipeline-actions/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
