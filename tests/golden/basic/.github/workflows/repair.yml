# GENERATED by lockstep 0.1.0 — do not edit.
# Edit the spec, add an overlay, or run `lockstep eject` on this file.
# sources: commands/repair.md@daa544cc pipeline.yaml@4feed875
# profile: my-app
name: repair
on:
  workflow_dispatch:
    inputs:
      attempt:
        description: Label for this repair attempt
        type: string
        default: ''
      force:
        description: Re-run steps even when a cached output exists
        type: boolean
        default: false
      force_steps:
        description: Comma-separated step ids to force
        type: string
        default: ''
  workflow_call:
    inputs:
      attempt:
        type: string
        default: ''
      force:
        type: boolean
        default: false
      force_steps:
        type: string
        default: ''
    secrets:
      APP_PASSWORD:
        required: false
      JIRA_API_TOKEN:
        required: false
    outputs:
      converged:
        description: Whether this run reached convergence
        value: ${{ jobs.check-convergence.outputs.converged }}
permissions:
  contents: read
concurrency:
  group: repair-my-app
  cancel-in-progress: false
env:
  OUTPUT_DIR: outputs
jobs:
  collect-failures:
    environment: my-app-staging
    runs-on: ubuntu-24.04
    container: ghcr.io/pipeline-fw/exec@sha256:71c2f5a9d3e84b6c0a1f7e2d5b8c9a4e3f6d0b1c2a5e8f7d4c3b6a9e0f1d2c3b
    outputs:
      items_repair_each_failing_script: ${{ steps.fanout-repair-each-failing-script.outputs.items }}
    env:
      MY_URL: ${{ vars.APP_URL }}
      PROFILE_URL: ${{ vars.APP_URL }}
      MY_API_URL: ${{ vars.API_URL }}
      PROFILE_API_URL: ${{ vars.API_URL }}
      MY_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      PROFILE_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      MY_PASSWORD: ${{ secrets.APP_PASSWORD }}
      PROFILE_PASSWORD: ${{ secrets.APP_PASSWORD }}
      MY_AUTH_METHOD: jwt
      PROFILE_AUTH_METHOD: jwt
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
    - uses: pipeline-fw/pipeline-actions/restore@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
    - id: cache-collect-failures
      uses: pipeline-fw/pipeline-actions/step-cache@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
      with:
        step: collect-failures
        key-prefix: ls-v1-user-story-validation-repair-collect-failures-372b2a80
        key-inputs: |
          .pipeline/step-defs/repair.collect-failures.json
          scripts/collect-failures.py
        outputs: |
          outputs/failures.json
        force: ${{ inputs.force }}
        force-steps: ${{ inputs.force_steps }}
    - name: Collect failures
      id: collect-failures
      if: ${{ steps.cache-collect-failures.outputs.hit != 'true' }}
      run: uv run python3 scripts/collect-failures.py --output=outputs/failures.json
    - name: Publish collect-failures outputs
      if: ${{ steps.cache-collect-failures.outputs.hit != 'true' }}
      uses: pipeline-fw/pipeline-actions/step-cache/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
      with:
        step: collect-failures
        outputs: |
          outputs/failures.json
    - name: Fan out failures
      id: fanout-repair-each-failing-script
      run: pipeline-exec fanout --input=outputs/failures.json --key=key --max=256 --only-missing --output-dir=outputs/repairs --shard-threshold=20 >> "$GITHUB_OUTPUT"
    - id: save-workspace
      if: ${{ always() }}
      uses: pipeline-fw/pipeline-actions/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
  repair-each-failing-script:
    needs: collect-failures
    environment: my-app-staging
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        item: ${{ fromJSON(needs.collect-failures.outputs.items_repair_each_failing_script) }}
    runs-on: ubuntu-24.04
    container: ghcr.io/pipeline-fw/exec@sha256:71c2f5a9d3e84b6c0a1f7e2d5b8c9a4e3f6d0b1c2a5e8f7d4c3b6a9e0f1d2c3b
    env:
      MY_URL: ${{ vars.APP_URL }}
      PROFILE_URL: ${{ vars.APP_URL }}
      MY_API_URL: ${{ vars.API_URL }}
      PROFILE_API_URL: ${{ vars.API_URL }}
      MY_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      PROFILE_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      MY_PASSWORD: ${{ secrets.APP_PASSWORD }}
      PROFILE_PASSWORD: ${{ secrets.APP_PASSWORD }}
      MY_AUTH_METHOD: jwt
      PROFILE_AUTH_METHOD: jwt
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
    - uses: pipeline-fw/pipeline-actions/restore@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
    - name: Repair each failing script
      id: repair-each-failing-script
      run: pipeline-exec shard-run --slice='${{ toJSON(matrix.item) }}' --input=outputs/failures.json --key=key -- uv run python3 scripts/repair-script.py --failure={item.key} --output=outputs/repairs/{item.key}.json
    - id: save-workspace
      if: ${{ always() }}
      uses: pipeline-fw/pipeline-actions/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
  check-convergence:
    needs: repair-each-failing-script
    environment: my-app-staging
    runs-on: ubuntu-24.04
    container: ghcr.io/pipeline-fw/exec@sha256:71c2f5a9d3e84b6c0a1f7e2d5b8c9a4e3f6d0b1c2a5e8f7d4c3b6a9e0f1d2c3b
    outputs:
      converged: ${{ steps.check-convergence.outputs.converged }}
    env:
      MY_URL: ${{ vars.APP_URL }}
      PROFILE_URL: ${{ vars.APP_URL }}
      MY_API_URL: ${{ vars.API_URL }}
      PROFILE_API_URL: ${{ vars.API_URL }}
      MY_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      PROFILE_JIRA_BASE_URL: ${{ vars.JIRA_BASE_URL }}
      MY_PASSWORD: ${{ secrets.APP_PASSWORD }}
      PROFILE_PASSWORD: ${{ secrets.APP_PASSWORD }}
      MY_AUTH_METHOD: jwt
      PROFILE_AUTH_METHOD: jwt
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
    - uses: pipeline-fw/pipeline-actions/restore@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
    - name: Check convergence
      id: check-convergence
      run: uv run python3 scripts/check-convergence.py --input=outputs/failures.json
    - id: save-workspace
      if: ${{ always() }}
      uses: pipeline-fw/pipeline-actions/save@8c44e0d2ab19f3c5d7e6b4a2091f83cc55d1e470  # v1.6.2
