# GENERATED by lockstep 0.1.0 — do not edit.
# Edit the spec, add an overlay, or run `lockstep eject` on this file.
# sources: commands/validate.md@07878f25 pipeline.yaml@4feed875
# profile: my-app
name: validate
on:
  workflow_dispatch:
    inputs:
      skip_repair:
        description: Run tests only, with no repair loop
        type: boolean
        default: false
      force:
        description: Re-run steps even when a cached output exists
        type: boolean
        default: false
      force_steps:
        description: Comma-separated step ids to force
        type: string
        default: ''
  schedule:
  - cron: 0 3 * * *
  workflow_call:
    inputs:
      skip_repair:
        type: boolean
        default: false
      force:
        type: boolean
        default: false
      force_steps:
        type: string
        default: ''
    secrets:
      APP_PASSWORD:
        required: false
      JIRA_API_TOKEN:
        required: false
permissions:
  contents: read
concurrency:
  group: validate-my-app
  cancel-in-progress: false
env:
  OUTPUT_DIR: outputs
jobs:
  generate-test-scripts:
    uses: ./.github/workflows/generate-tests.yml
    with:
      force: ${{ inputs.force }}
    secrets:
      APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
  repair-loop-1:
    needs: generate-test-scripts
    if: ${{ inputs.skip_repair != true }}
    uses: ./.github/workflows/repair.yml
    with:
      force: ${{ inputs.force }}
    secrets:
      APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
  repair-loop-2:
    needs: repair-loop-1
    if: ${{ !failure() && !cancelled() && inputs.skip_repair != true && needs.repair-loop-1.outputs.converged != 'true' }}
    uses: ./.github/workflows/repair.yml
    with:
      force: ${{ inputs.force }}
    secrets:
      APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
  repair-loop-3:
    needs:
    - repair-loop-1
    - repair-loop-2
    if: ${{ !failure() && !cancelled() && inputs.skip_repair != true && needs.repair-loop-1.outputs.converged != 'true' && needs.repair-loop-2.outputs.converged != 'true' }}
    uses: ./.github/workflows/repair.yml
    with:
      force: ${{ inputs.force }}
    secrets:
      APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
