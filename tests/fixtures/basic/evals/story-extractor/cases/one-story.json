{
  "input": {
    "key": "APP-77",
    "summary": "Reject an order with no line items",
    "description": "Submitting an empty order currently returns 200 and creates nothing."
  },
  "expect": {
    "schema": ["stories"],
    "count": { "stories": { "min": 1 } },
    "absent": ["TODO"],
    "rubric": "One story per behaviour the issue describes, each with an acceptance criterion that could fail."
  }
}
