"""Implementations of the protocols `core/ports` declares.

Nothing in `core` imports this package. That is the layering rule, and it is what keeps
`RunContext` from depending on the things it abstracts over.
"""
