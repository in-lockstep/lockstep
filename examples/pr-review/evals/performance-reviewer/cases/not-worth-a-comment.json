{
  "input": {
    "pull_request": {
      "title": "Tidy the currency formatter",
      "body": "Readability only.",
      "head_sha": "b8c9d0e1",
      "files": [
        {
          "path": "src/money.py",
          "patch": "@@ -4,3 +4,5 @@ def fmt(amount, currency):\n-    return \"%s%s\" % (SYMBOLS[currency], amount)\n+    symbol = SYMBOLS[currency]\n+    return f\"{symbol}{amount}\""
        }
      ]
    },
    "revision": false
  },
  "expect": {
    "notes": "Reports nothing. String formatting on a value-sized input is a micro-optimisation, and this lens is told not to report those."
  }
}
