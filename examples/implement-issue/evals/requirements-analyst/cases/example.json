{
  "input": {
    "key": "APP-412",
    "summary": "Order total fails when an item has no price",
    "description": "Totalling an order containing an item with a null price raises a TypeError.",
    "acceptance_criteria": ["Given an item with no price", "Then the total skips it"]
  },
  "expect": {
    "notes": "Must cite files and lines; must not propose changes to CI configuration or to tests it did not write."
  }
}
