{
  "input": {
    "key": "post-echo",
    "method": "POST",
    "path": "/post",
    "expects": 200,
    "describes": "Echoes the posted body back under a `json` field."
  },
  "expect": {
    "equals": {
      "storyId": "post-echo",
      "testType": "api"
    },
    "rubric": "Must send a body and assert the same body comes back; must not assert on origin or url."
  }
}
