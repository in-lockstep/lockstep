# GENERATED by lockstep 0.1.0 — do not edit.
# Edit the spec, add an overlay, or run `lockstep eject` on this file.
# sources: retro:commands/retro.md@dc884317 .lockstep/pipeline.yaml@8f56afbc
# profile: repo
name: retro/retro
on:
  workflow_dispatch:
    inputs:
      days:
        description: How far back the window reaches
        type: string
        default: '30'
      branch:
        description: The branch the run ledger lives on
        type: string
        default: pipeline-history
      force:
        description: Re-run steps even when a cached output exists
        type: boolean
        default: false
      force_steps:
        description: Comma-separated step ids to force
        type: string
        default: ''
  schedule:
  - cron: 0 6 * * 1
  workflow_call:
    inputs:
      days:
        type: string
        default: '30'
      branch:
        type: string
        default: pipeline-history
      force:
        type: boolean
        default: false
      force_steps:
        type: string
        default: ''
permissions:
  contents: read
concurrency:
  group: retro/retro-repo
  cancel-in-progress: false
env:
  OUTPUT_DIR: outputs
jobs:
  history:
    permissions:
      contents: read
      actions: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --cap-add=DAC_OVERRIDE --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@45f5a996ffbe35ba03ee18d540de6dcd3f9c6011  # actions-v0.1.1
    - id: cache-history
      uses: in-lockstep/lockstep/actions/step-cache@45f5a996ffbe35ba03ee18d540de6dcd3f9c6011  # actions-v0.1.1
      with:
        step: history
        key-prefix: ls-v1-lockstep-retro-retro-history-4f53cda1
        key-inputs: |
          .lockstep/.pipeline/step-defs/retro/retro.history.json
        outputs: |
          outputs/history.json
        force: ${{ inputs.force }}
        force-steps: ${{ inputs.force_steps }}
    - name: Read what the pipelines did
      id: history
      if: ${{ steps.cache-history.outputs.hit != 'true' }}
      run: pipeline-exec run-history --branch="${{ inputs.branch || 'pipeline-history' }}" --days="${{ inputs.days || '30' }}" --output=outputs/history.json
    - name: Publish history outputs
      if: ${{ steps.cache-history.outputs.hit != 'true' }}
      uses: in-lockstep/lockstep/actions/step-cache/save@45f5a996ffbe35ba03ee18d540de6dcd3f9c6011  # actions-v0.1.1
      with:
        step: history
        outputs: |
          outputs/history.json
        key: ${{ steps.cache-history.outputs.key }}
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@45f5a996ffbe35ba03ee18d540de6dcd3f9c6011  # actions-v0.1.1
  scan-say-what-to-change:
    name: Scan input for retro/retro-analyst
    needs: history
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --cap-add=DAC_OVERRIDE --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@45f5a996ffbe35ba03ee18d540de6dcd3f9c6011  # actions-v0.1.1
    - name: Scan for hidden instructions
      id: scan
      run: pipeline-exec scan-input --input=outputs/history.json --mode=warn --report=outputs/scan/say-what-to-change.json
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@45f5a996ffbe35ba03ee18d540de6dcd3f9c6011  # actions-v0.1.1
  say-what-to-change:
    needs:
    - history
    - scan-say-what-to-change
    permissions:
      actions: read
      contents: read
      issues: write
    uses: ./.github/workflows/aw-retro-retro-analyst.lock.yml
    with:
      input_path: outputs/history.json
      output_path: outputs/retro.json
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
  meter:
    name: Meter the run
    needs:
    - say-what-to-change
    if: ${{ !cancelled() }}
    permissions:
      contents: write
      actions: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --cap-add=DAC_OVERRIDE --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - name: Collect what each agent reported spending
      uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0  # v5
      with:
        pattern: '*usage*'
        path: outputs/usage
        merge-multiple: false
        if-no-files-found: ignore
      continue-on-error: true
    - name: Ask how the run went
      run: mkdir -p outputs/otel && gh api --paginate "/repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID/jobs" > outputs/otel/jobs.json || true
      env:
        GH_TOKEN: ${{ github.token }}
    - name: Write the rate table
      run: |-
        mkdir -p outputs/otel
        cat > outputs/otel/pricing.json <<'PRICING'
        {}
        PRICING
    - name: Price it
      id: meter
      run: pipeline-exec meter --usage=outputs/usage --output=outputs/otel/metrics.json --pricing=outputs/otel/pricing.json --jobs=outputs/otel/jobs.json --service-name=lockstep --title="Cost of `retro/retro`" --history-dir=outputs/history
    - name: Record the run
      uses: in-lockstep/lockstep/actions/publish-history@45f5a996ffbe35ba03ee18d540de6dcd3f9c6011  # actions-v0.1.1
      with:
        source: outputs/history
        branch: pipeline-history
        path: history
    continue-on-error: true
