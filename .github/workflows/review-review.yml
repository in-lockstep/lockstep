# GENERATED by lockstep 0.1.0 — do not edit.
# Edit the spec, add an overlay, or run `lockstep eject` on this file.
# sources: review:commands/review.md@76ce9d76 .lockstep/pipeline.yaml@6c1c37d8
# profile: repo
name: review/review
on:
  workflow_dispatch:
    inputs:
      pr:
        description: Pull request number, when dispatched rather than commented
        type: string
        default: ''
      force:
        description: Review again even where the pull request has not moved
        type: boolean
        default: false
      force_steps:
        description: Comma-separated step ids to force
        type: string
        default: ''
  issue_comment:
    types:
    - created
  pull_request_review_comment:
    types:
    - created
  workflow_call:
    inputs:
      pr:
        type: string
        default: ''
      force:
        type: boolean
        default: false
      force_steps:
        type: string
        default: ''
permissions:
  contents: read
concurrency:
  group: review/review-repo-${{ github.event.issue.number || github.event.pull_request.number || github.ref }}
  cancel-in-progress: false
env:
  OUTPUT_DIR: outputs
jobs:
  command-gate:
    name: Authorize /review
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    outputs:
      authorized: ${{ steps.gate.outputs.authorized }}
      instruction: ${{ steps.gate.outputs.instruction }}
      pull_request: ${{ steps.gate.outputs.pull_request }}
      positional: ${{ steps.gate.outputs.positional }}
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - name: Authorize /review
      id: gate
      uses: in-lockstep/lockstep/actions/command-gate@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
      with:
        command: /review
        roles: admin,maintain,write
        associations: OWNER,MEMBER,COLLABORATOR
        arguments: ''
        reaction: eyes
    - name: Expand the parsed arguments
      id: arguments
      run: |
        set -euo pipefail
        payload='${{ steps.gate.outputs.arguments }}'
        [ -n "$payload" ] || payload="{}"
      shell: bash
  diff:
    needs: command-gate
    if: ${{ needs.command-gate.outputs.authorized == 'true' }}
    permissions:
      contents: read
      actions: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --security-opt=no-new-privileges
    outputs:
      pending: ${{ steps.state.outputs.pending }}
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
    - id: cache-diff
      uses: in-lockstep/lockstep/actions/step-cache@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
      with:
        step: diff
        key-prefix: ls-v1-lockstep-review/review-diff-4f53cda1
        key-inputs: |
          .lockstep/.pipeline/step-defs/review/review.diff.json
        outputs: |
          outputs/diff.json
        force: ${{ inputs.force }}
        force-steps: ${{ inputs.force_steps }}
    - name: Fetch the pull request
      id: diff
      if: ${{ steps.cache-diff.outputs.hit != 'true' }}
      run: pipeline-exec pr-diff --pr="${{ needs.command-gate.outputs.pull_request }}" --output=outputs/diff.json
    - name: Publish diff outputs
      if: ${{ steps.cache-diff.outputs.hit != 'true' }}
      uses: in-lockstep/lockstep/actions/step-cache/save@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
      with:
        step: diff
        outputs: |
          outputs/diff.json
        key: ${{ steps.cache-diff.outputs.key }}
    - name: Work out which reviews are still due
      id: state
      run: pipeline-exec review-state --pr="${{ needs.command-gate.outputs.pull_request }}" --requested="${{ needs.command-gate.outputs.positional }}" --available=security,intent,performance,tests --output-dir=outputs/pending
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
  scan-review-for-security:
    name: Scan input for review/security-reviewer
    needs:
    - command-gate
    - diff
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'security') }}
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
    - name: Scan for hidden instructions
      id: scan
      run: pipeline-exec scan-input --input=outputs/pending/security.json --input=outputs/diff.json --mode=warn --report=outputs/scan/review-for-security.json
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
  review-for-security:
    needs:
    - command-gate
    - diff
    - scan-review-for-security
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'security') }}
    permissions:
      actions: read
      contents: read
      issues: write
    uses: ./.github/workflows/aw-review-security-reviewer.lock.yml
    with:
      input_path: outputs/pending/security.json
      output_path: outputs/reviews/security.json
      context_files: outputs/diff.json
  scan-review-for-intent:
    name: Scan input for review/intent-reviewer
    needs:
    - command-gate
    - diff
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'intent') }}
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
    - name: Scan for hidden instructions
      id: scan
      run: pipeline-exec scan-input --input=outputs/pending/intent.json --input=outputs/diff.json --mode=warn --report=outputs/scan/review-for-intent.json
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
  review-for-intent:
    needs:
    - command-gate
    - diff
    - scan-review-for-intent
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'intent') }}
    permissions:
      actions: read
      contents: read
      issues: write
    uses: ./.github/workflows/aw-review-intent-reviewer.lock.yml
    with:
      input_path: outputs/pending/intent.json
      output_path: outputs/reviews/intent.json
      context_files: outputs/diff.json
  scan-review-for-performance:
    name: Scan input for review/performance-reviewer
    needs:
    - command-gate
    - diff
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'performance') }}
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
    - name: Scan for hidden instructions
      id: scan
      run: pipeline-exec scan-input --input=outputs/pending/performance.json --input=outputs/diff.json --mode=warn --report=outputs/scan/review-for-performance.json
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
  review-for-performance:
    needs:
    - command-gate
    - diff
    - scan-review-for-performance
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'performance') }}
    permissions:
      actions: read
      contents: read
      issues: write
    uses: ./.github/workflows/aw-review-performance-reviewer.lock.yml
    with:
      input_path: outputs/pending/performance.json
      output_path: outputs/reviews/performance.json
      context_files: outputs/diff.json
  scan-review-for-test-coverage:
    name: Scan input for review/tests-reviewer
    needs:
    - command-gate
    - diff
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'tests') }}
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
    - name: Scan for hidden instructions
      id: scan
      run: pipeline-exec scan-input --input=outputs/pending/tests.json --input=outputs/diff.json --mode=warn --report=outputs/scan/review-for-test-coverage.json
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
  review-for-test-coverage:
    needs:
    - command-gate
    - diff
    - scan-review-for-test-coverage
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' && needs.diff.outputs.pending != '' && contains(fromJSON(needs.diff.outputs.pending), 'tests') }}
    permissions:
      actions: read
      contents: read
      issues: write
    uses: ./.github/workflows/aw-review-tests-reviewer.lock.yml
    with:
      input_path: outputs/pending/tests.json
      output_path: outputs/reviews/tests.json
      context_files: outputs/diff.json
  post:
    needs:
    - command-gate
    - review-for-security
    - review-for-intent
    - review-for-performance
    - review-for-test-coverage
    if: ${{ !failure() && !cancelled() && needs.command-gate.outputs.authorized == 'true' }}
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - uses: in-lockstep/lockstep/actions/restore@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
    - name: Post one review per aspect
      id: post
      run: pipeline-exec post-reviews --pr="${{ needs.command-gate.outputs.pull_request }}" --reviews=outputs/reviews --pending=outputs/pending --diff=outputs/diff.json
    - id: save-workspace
      if: ${{ always() }}
      uses: in-lockstep/lockstep/actions/save@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
  meter:
    name: Meter the run
    needs:
    - command-gate
    - review-for-intent
    - review-for-performance
    - review-for-security
    - review-for-test-coverage
    if: ${{ needs.command-gate.outputs.authorized == 'true' && !cancelled() }}
    permissions:
      contents: write
      actions: read
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/in-lockstep/pipeline-exec@sha256:70de3f808f16cf308864107c0659c08245dee1910aab50c38c20cdb684fc7253
      options: --cap-drop=ALL --security-opt=no-new-privileges
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
    - name: Collect what each agent reported spending
      uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0  # v5
      with:
        pattern: '*usage*'
        path: outputs/usage
        merge-multiple: false
        if-no-files-found: ignore
      continue-on-error: true
    - name: Ask how the run went
      run: mkdir -p outputs/otel && gh api --paginate "/repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID/jobs" > outputs/otel/jobs.json || true
      env:
        GH_TOKEN: ${{ github.token }}
    - name: Write the rate table
      run: |-
        mkdir -p outputs/otel
        cat > outputs/otel/pricing.json <<'PRICING'
        {}
        PRICING
    - name: Price it
      id: meter
      run: pipeline-exec meter --usage=outputs/usage --output=outputs/otel/metrics.json --pricing=outputs/otel/pricing.json --jobs=outputs/otel/jobs.json --service-name=lockstep --title="Cost of `review/review`" --history-dir=outputs/history
    - name: Record the run
      uses: in-lockstep/lockstep/actions/publish-history@aad2f112b8263ca9cf9a6169e1247d02d6ba9e7a  # actions-v0.1.0
      with:
        source: outputs/history
        branch: pipeline-history
        path: history
    continue-on-error: true
